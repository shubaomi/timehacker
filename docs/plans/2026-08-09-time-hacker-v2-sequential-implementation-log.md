# Time Hacker V2：逐关实施与验收台账

> 开始日期：2026-08-09
>
> 当前状态：100 / 100 工程验收通过；未知答案真人验收 0 / 100
>
> 执行规则：一关实现、一关验证、一关记录；当前关未通过不得把下一关标记完成

## 1. 完成口径

每关只有同时满足以下工程硬门，才能记为“工程验收通过”：

1. 实现逐条对应设计圣经中的场景、发现、破解、反馈、跨端与验收合同。
2. 未破解前不显示内部名称、答案、步骤卡、进度数字或统一发现入口。
3. 合理操作在 150ms 内得到与场景因果一致的反馈；机械遍历明显对象不得误通关。
4. 难点来自观察和推理，不来自像素精度、毫秒反应、冗长路径或重复次数。
5. 桌面、手机和键盘均有语义等价路线；权限、传感器和页面外能力不是唯一解。
6. 谜题元素不遮挡挑战文案、秒表和主按钮；`ARMED` 后场景收束且不拦截计时。
7. 至少包含专属组件测试和桌面/手机真实浏览器测试；全局静态检查保持通过。

“未知答案真人验收通过”单独记录，必须有未读答案玩家证据；自动化不能替代该结论。

## 2. 总览

| 状态 | 数量 |
| --- | ---: |
| 工程验收通过 | 100 |
| 实现中 | 0 |
| 待逐关重构 | 0 |
| 未知答案真人验收通过 | 0 |

下一步：执行 100 关全量回归、数据库配置核对与发布前检查；不再新增关卡范围。

### 001–012 批次回归里程碑

- Playwright `desktop-1440`：12 / 12 通过。
- Playwright `mobile-390`：12 / 12 通过。
- 合计：24 / 24 通过；逐关错误路径、正确路径、视口边界与核心区不遮挡断言均保持绿色。
- 结论：阶段门 001–012 的工程实现可以作为稳定基线；未知答案真人验收仍为 0 / 12，不能以自动化替代。
- 编号校正：运行时注册表与设计圣经均确认 011 为 `double-relay`，`quiet-circuit` 为 020；后续严格按设计圣经编号推进。

### 013–016 批次回归里程碑

- Playwright `desktop-1440`：4 / 4 通过。
- Playwright `mobile-390`：4 / 4 通过。
- 合计：8 / 8 通过；覆盖波形零点、双轴十字、交替圆环和移动尾迹的专属错误/正确路径、视口边界与核心区不遮挡。
- 001–016 合并关键路径门在开发态被 240 秒外层命令上限终止，未产生关卡失败证据，因此未记为通过；保留既有 001–012 绿色里程碑，并以本次 013–016 双端专属回归作为新增批次证据。

### 017–020 批次回归里程碑

- Playwright `desktop-1440`：4 / 4 通过。
- Playwright `mobile-390`：4 / 4 通过。
- 合计：8 / 8 通过；覆盖反向纸片、内外边界、双壳夹层和绕开伪按钮的专属错误/正确路径、视口边界与核心区不遮挡。

### 021–024 批次回归里程碑

- Playwright `desktop-1440`：4 / 4 通过。
- Playwright `mobile-390`：4 / 4 通过。
- 合计：8 / 8 通过；覆盖呼吸框交会、缺失墨滴、花瓣空隙匹配和曲线路程节拍的专属错误/正确路径、视口边界与核心区不遮挡。
- 首轮桌面批次在 021/022 的窗口末端遭遇 Playwright 可操作性等待，点击落到窗外但均未误通关；把“已确认交会后的点击”改为页面事件循环内立即触发同一浏览器 `click` 事件后，完整 8 项稳定通过。精确时间边界仍由假时钟组件测试覆盖。

### 025–032 批次回归里程碑

- Playwright `desktop-1440`：8 / 8 通过。
- Playwright `mobile-390`：8 / 8 通过。
- 合计：16 / 16 通过；覆盖暗拍窗口、五点分组、四分圆轨、反拍纸花、检查尺缺口、背面折线、可调内窗和标题铰链的专属错误/正确路径、视口边界与核心区不遮挡。
- 首轮批次在手机 025 暴露“DOM 仍显示等待、内部 50ms 时钟引用已进入有效窗”的竞争，导致一次本应记录失败的点击被内部接受。判定已改为当前渲染轮廓状态，与玩家实际看到的粗/细边一致；025 手机专属复测及完整 16 项批次随后全部通过。

## 3. 逐关记录

### 001 `four-corner-breach` — 工程验收通过

- **设计事实**：整页纸框存在真实缺角；游离纸角的形状和方向与缺口吻合。拖动超过 12px 才进入发现状态，把纸角放入带 22px 宽容区的二维缺口即破解；单击不得通关。
- **实现结果**：删除固定横向轨道、虚线滑道和通用 `DragRelation`；改为整页边框、真实缺口和自由二维纸角。错误落点产生折痕并复位；键盘用三次向右加一次向下表达相同空间关系。
- **信息层级**：未破解前只显示关卡编号，不显示“四角突破”；纸角与缺口位于页面外围，不覆盖挑战标题、秒表或主按钮；移除本关随机装饰图标。
- **跨端**：桌面纸角位于左侧留白，手机纸角重排到底部留白；手机缺口缩小并移到标题上方右侧安全区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：3 / 3 通过。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过。
  - 浏览器控制台：专属验收无页面错误。
- **仍缺证据**：未知答案玩家能否由“缺口与外角形状一致”推断解法；物理手机拖动手感。
- **停止结论**：工程硬门已通过，不继续做纯配色或装饰微调；进入 002。

### 002 `breath-gap` — 工程验收通过

- **设计事实**：点击、按键、滚轮、触摸和拖动都会让两片纸叶重新闭合；普通鼠标移动不算打扰。安静 1 秒后纸叶开始呼吸，累计 2.5 秒前台安静时间才露出气泡；长按 1.2 秒破解。
- **实现结果**：用可暂停的前台安静计时替换旧固定 `setTimeout`。四类有意义输入会从零重启发现窗口；页面隐藏会暂停剩余时间，返回页面不能直接完成。650ms 提前释放只让气泡缩回，气泡仍保留，可立即再次尝试。
- **信息层级**：未破解前不显示“呼吸间隙”；移除随机环境图标。两片纸叶与无按钮外观的薄荷气泡只占秒表上方的窄缝，场景唯一循环动画是安静后的呼吸。
- **跨端**：鼠标/触摸长按与 Space 长按等价；`pointercancel`、窗口失焦和卸载会取消长按。利用实际秒表矩形计算纸叶位置，桌面与手机都不覆盖挑战、秒表或主按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：10 / 10 通过，其中 7 项覆盖 002 的时间与输入合同。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过。
  - 截图：`artifacts/screenshots/v2-sequential/002-desktop-1440.png`、`002-mobile-390.png`。
- **仍缺证据**：未知答案玩家能否在 30 秒内注意到“越操作越闭”的因果；物理手机长按和浏览器切后台手感。
- **停止结论**：工程硬门已通过；不通过缩短等待或增加按钮提示继续美化，进入 003。

### 003 `slow-command` — 工程验收通过

- **设计事实**：`FAST` 四块活字纸牌比页面其他物体漂得快；玩家通过语义推断把世界规则改写成 `SLOW`。每列目标字母距起点两格，所有牌各机械翻一次只能得到 `TIME`，不得误通关；`SLOW` 稳定 400ms 才完成。
- **实现结果**：保留四列独立候选字母，补齐点击、滚轮、上下键和单指上下轻扫；没有文本框、软键盘、提交按钮或口令。字牌点击区域保持静止，只有字形轻微漂动，避免动画把操作变成命中测试；正确字母逐渐减速并与慢影同步。
- **信息层级**：未破解前不显示“慢词机关”，移除随机装饰标记。桌面和手机都把 `FAST` 纸牌排在主按钮下方独立空间，不覆盖标题、秒表或主按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：12 / 12 通过，其中 2 项覆盖 003 的语义、误通关和四种输入路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过。
  - 截图：`artifacts/screenshots/v2-sequential/003-desktop-1440.png`、`003-mobile-390.png`。
- **仍缺证据**：未知答案中文玩家能否在未开二级提示时理解 `FAST → SLOW`；字形轻微漂动是否足以被察觉而不干扰点击。
- **停止结论**：工程硬门已通过；不增加拖线、提交或第二阶段动作，进入 004。

### 004 `target-knock` — 工程验收通过

- **设计事实**：三枚圆纸片的可见大小顺序与其影长顺序不同；拖动纸片时影子留在原位，玩家应按影长而不是按纸片大小，把三枚纸片放入短、中、长三个浅槽。浅槽画面顺序刻意打乱为长、短、中，按大小从左到右机械排列不得通关。
- **实现结果**：删除“任意纸片向下拖一次就完成”的通用逻辑；改为自由二维拖动、静止影子和独立命中浅槽。错误浅槽在 150ms 内收紧变红并让纸片复位；正确关系为大纸片→短影槽、中纸片→中影槽、小纸片→长影槽。键盘左右选择浅槽、Enter 放置，与拖放语义等价。
- **信息层级**：未破解前不显示“影子排队”，移除本关随机图标和统一轨道。专属斜向晨光只解释影子来源；圆片和浅槽分置主按钮左右（手机端排在按钮下方），不遮挡挑战标题、秒表或主按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：14 / 14 通过，其中 2 项覆盖错误大小顺序、正确影长映射、拖动物体时影子静止和发现回调。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；同时断言关卡元素位于视口内且不覆盖核心游戏区。
  - 截图：`artifacts/screenshots/v2-sequential/004-desktop-1440.png`、`004-mobile-390.png`。
- **仍缺证据**：未知答案玩家能否仅凭“纸片移动而影子不动”推断应相信影长；物理手机把纸片拖入浅槽时的手感。
- **停止结论**：工程硬门已通过；不增加文字步骤或显式连线，进入 005。

### 005 `amber-triangle` — 工程验收通过

- **设计事实**：三个外形不同的纸砝码真实重量相同，偏在一侧的琥珀灯让三道影长不等并制造“纸架失衡”的错觉。尝试移动单个砝码只能扰动它的影子，不能改变纸架；移动共同光源，让三道影长进入宽容等长区才完成。
- **实现结果**：删除通用横向场景滑块；改为专属三角纸架、三种等面积纸砝码、三道动态影子和独立琥珀灯。灯位由 0–100 的连续空间控制，等长允许 8px 影长差，不要求单像素命中；键盘每次移动 10%，第四次向右进入中心宽容区。拖砝码和 Enter 只让对应影子短暂偏移，不会误通关。
- **信息层级**：未破解前不显示“琥珀平衡”，移除随机环境图标。桌面把纸架和光源分置秒表左右；手机把纸架及光源依次排在主按钮下方，光晕只说明共同光源，不覆盖核心游戏区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：16 / 16 通过，其中 2 项覆盖砝码伪操作不改变纸架/不通关，以及三影等长宽容区。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；断言纸架和灯轨均在视口内且不覆盖挑战、秒表或主按钮。
  - 截图：`artifacts/screenshots/v2-sequential/005-desktop-1440.png`、`005-mobile-390.png`。
- **仍缺证据**：未知答案玩家能否从“砝码不动而影子动”推断光源才是共同控制物；物理手机连续拖灯的手感。
- **停止结论**：工程硬门已通过；不增加水平仪读数、数值提示或显式目标区，进入 006。

### 006 `patient-zero` — 工程验收通过

- **设计事实**：雾纸内藏着闭合零形，玩家擦过正确轨道时才留下较持久的清晰带；目标是沿零形连续覆盖，不是把整张雾纸擦满，也不是累计任意移动距离。
- **实现结果**：删除“任意跨度足够大的闭环”判定和通用曲线路径；改为 27×35 的隐藏椭圆、13 个归一化单位的宽容带、八个方向扇区和闭合端点约束。至少 75% 采样落在轨道宽容带、覆盖至少七个扇区并闭合才通过；四角大范围乱涂会明确失败并让雾重新合拢。键盘按左、下、右、上的一周路线提供语义等价路径。
- **信息层级**：未破解前不显示“雾中零点”，移除随机装饰标记。雾纸只保留一条极淡、随雾呼吸显现的零形纸纹；桌面位于秒表右侧，手机位于主按钮下方，不覆盖核心游戏区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：18 / 18 通过，其中 2 项覆盖无意义大闭环失败、宽松零形轨道成功、八扇区揭示和发现回调。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；在真实浏览器中再次验证乱涂失败、键盘闭环成功、视口和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/006-desktop-1440.png`、`006-mobile-390.png`。
- **仍缺证据**：未知答案玩家能否把淡零形识别为可擦拭轨道；不同触屏采样率下轨迹宽容是否仍自然。
- **停止结论**：工程硬门已通过；不增加进度数字、擦除百分比或显式起点，进入 007。

### 007 `window-peek` — 工程验收通过

- **设计事实**：空窗框留在纸面内，完整窗影却被页面右边缘截断。窗影是诱饵：拖它只会拉长投影；应移动投下它的空窗框，让框与边缘外的影子重新对齐。
- **实现结果**：删除通用横向滑块和中央目标圈；改为真实页面右边缘、可抓取空窗框和只露出半边的框外窗影。拖/点击窗影只触发 180ms 拉伸反馈，不会通关；窗框进入 76%–100% 的宽松边缘吸附区自动完成。键盘四次向右到 80%，单击及前三次移动均不得误通关。
- **信息层级**：未破解前不显示“框外窗影”，移除随机装饰图标。桌面把谜面放在秒表右侧并让视口边缘参与构图；手机把窗框与截断窗影排在主按钮下方两端，不遮挡核心游戏区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：20 / 20 通过，其中 2 项覆盖窗影诱饵、150ms 量级反馈、单击失败和宽松边缘吸附。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证窗影失败、窗框路径成功、交互物完整位于视口内且不覆盖核心区。
  - 截图：`artifacts/screenshots/v2-sequential/007-desktop-1440.png`、`007-mobile-390.png`。
- **仍缺证据**：未知答案玩家是否会把“影子被页面裁掉”理解为应移动窗框；物理手机拖到屏幕边缘时是否会触发系统返回手势。
- **停止结论**：工程硬门已通过；不增加箭头、虚线轨道或显式边缘目标圈，进入 008。

### 008 `relay-sandwich` — 工程验收通过

- **设计事实**：两块珊瑚外壳有互补半圆缺口，一块透明薄片材质不同。薄片靠近时两侧外壳共同回应；正确结构只能是透明片居中、外壳从两侧合拢，随意把三件堆在一起不得通过。
- **实现结果**：删除“三层各操作两次”的通用层叠器；改为三件自由拖放纸件、共享中央吸附区和严格的中间层状态。外壳先行会在 180ms 内弹开；透明片居中后，左右外壳可分别锁定，另一侧错误不会清除已经正确的一侧。中心命中额外保留 20px 宽容，键盘以透明片 Enter、左壳向右、右壳向左表达相同空间结构。
- **信息层级**：未破解前不显示“继电器夹层”，移除随机装饰标记。材质、缺口和两侧共同响应承担提示，不显示步骤或层级数字；桌面放在秒表左侧，手机放在主按钮下方，三件纸件均不覆盖核心区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：22 / 22 通过，其中 2 项覆盖错误顶层拒绝、正确一侧保留、完整层序成功和透明片触发双侧响应。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；移动端验收曾发现首次布局测量前的瞬时坐标，测试现等待实际秒表矩形稳定后再核验，最终三件交互区均不覆盖核心内容。
  - 截图：`artifacts/screenshots/v2-sequential/008-desktop-1440.png`、`008-mobile-390.png`。
- **仍缺证据**：未知答案玩家能否仅凭材质和互补缺口推断透明层应夹在中间；物理手机三件拖放及吸附的触觉清晰度。
- **停止结论**：工程硬门已通过；不增加层级编号、装配虚线或确认按钮，进入 009。

### 009 `clue-cipher` — 工程验收通过

- **设计事实**：四块正面字块按 `MTEI` 无意义顺序散落，固定在浅窝里的影字却从左到右读作 `TIME`。拖动正面字块时影字不跟随；应把每块字形送到相同影字位置，最终正面也读成 `TIME`。
- **实现结果**：删除只有三枚无字圆片的通用影子排序器；改为四块独立活版字块、四个固定影字浅窝和严格字形映射。机械按当前正面顺序从左放置时，首块 `M` 在 `T` 影位立即失败；正确映射为 M→2、T→0、E→3、I→1。拖放容差为浅窝外扩 18px，键盘可左右选择影位并 Enter 放置，全程无输入框、口令或提交按钮。
- **信息层级**：未破解前不显示“会说真话的影字”，移除随机装饰标记。斜置实体活字与淡灰固定影字构成唯一线索；桌面放在秒表右侧，手机排在主按钮下方，不覆盖核心游戏区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：24 / 24 通过，其中 2 项覆盖正面顺序失败、完整 TIME 映射成功、影字静止和无表单边界。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证四块交互区在视口内、不覆盖核心内容，错误首放失败、正确映射成功。
  - 截图：`artifacts/screenshots/v2-sequential/009-desktop-1440.png`、`009-mobile-390.png`。
- **仍缺证据**：未知答案玩家是否会把淡影字视为位置关系而非单纯装饰；物理手机四块拖放是否比键盘选择更自然。
- **停止结论**：工程硬门已通过；不增加输入框、显式连线或步骤顺序，进入 010。

### 010 `calibration-101` — 工程验收通过

- **设计事实**：左右页边各印一个细长 `1`，靠内侧各有一道互补半圆压痕。输入 `101`、点击数字或只折一边都不是答案；必须把两侧页边分别向内折，让两道半圆在中央合成 `0`，整张折纸才读成 `101`。
- **实现结果**：删除单张纸片重复点击的通用折叠器；改为左右两个独立页边、两道互补半圆和会随折叠收窄的整页轮廓。单侧折入只进入 `one-side` 状态且不会通关；双侧完成进入 `narrow`。鼠标/触摸向内拖动超过 56px 即宽松吸附，不要求精确停点；键盘左边向右、右边向左等价。普通点击和键盘数字均无处理。
- **信息层级**：未破解前不显示“折出的 101”，移除随机装饰标记。两个 `1` 与两道半圆直接印在页边上，无输入框、数字键盘、提交或步骤卡；交互位于主按钮下方，整页边线只作结构提示而不覆盖内容。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：26 / 26 通过，其中 2 项覆盖输入 101 无效、点击无效、单侧不通关、双侧成功和 56px 宽容拖折。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证两个页边在视口内、不覆盖核心区，并重复数字键、单侧和双侧路径。
  - 截图：`artifacts/screenshots/v2-sequential/010-desktop-1440.png`、`010-mobile-390.png`。
- **仍缺证据**：未知答案玩家是否会把两个半圆理解为要合成中间的 `0`；移动端从页边向内拖动是否与系统边缘手势冲突。
- **停止结论**：工程硬门已通过；不增加数字输入、折叠进度或显式中心目标，进入 011。

### 011 `double-relay` — 工程验收通过

- **设计事实**：两片纸叶以不同节奏翻面，背面具有相同纸纹；触碰任一纸叶只会让另一片延迟响应，从而暴露两者联动。控制关系位于两片之间，连续按住中央纸轴会逐步消除五级相位差；相位归零并稳定 300ms 后自动锁定，不考验松手时机。
- **实现结果**：用专属双叶继电场景替换通用长按气泡。左右纸叶使用不同翻面周期；触碰后的延迟响应持续 240ms，但不会减少相位差。中轴连续按住以每 160ms 一级的速度收束相位，约 1.1 秒自动完成；640ms 短按会恢复初始相位，轮流快速点击八次也不能误通关。Space 与指针长按语义等价。
- **信息层级**：未破解前不显示“同面继电”，移除随机环境图标。正反纸纹、不同翻面节奏与中轴长度承担线索，不显示进度数字、步骤或按钮文案；桌面置于秒表左下留白，手机置于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：29 / 29 通过，其中 3 项覆盖延迟联动、乱点失败、短按失败、稳定自动完成与 Space 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证三件交互物位于视口内且不覆盖挑战、秒表或主按钮。
  - 截图：`artifacts/screenshots/v2-sequential/011-desktop-1440.png`、`011-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否从“触碰一片、另一片延迟翻面”推断应按住中轴；物理手机长按时翻面收束是否足够清晰。
- **停止结论**：工程硬门已通过；不增加相位数字、长按文案或显式进度条，进入 012。

### 012 `pressure-delay` — 工程验收通过

- **设计事实**：两枚软纸圆盘相隔较远；按住一侧时，另一侧的压力波延迟鼓起。持续施压约 700ms 后两边波纹进入等半径区，再稳定保持 700ms 自动完成，不要求在某个瞬间松手。
- **实现结果**：删除通用长按中心，改为两枚独立圆盘与各自双层压力波。指针/触摸按住任一圆盘和 Space 均能启动同一路径；另一侧只在延迟推进后响应。420ms 短按会恢复分离状态，双击两侧不能通关；一轮连续约 1.4 秒长按自动完成。
- **信息层级**：未破解前不显示“压力回声”，移除随机环境图标。远距离圆盘、不同波纹半径和延迟鼓起承担线索，无倒计时、长按文案或完成按钮；桌面分置秒表两侧，手机并排位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：32 / 32 通过，其中 3 项用精确假时钟覆盖双击失败、延迟响应、短按失败、700ms 等半径稳定窗与 Space 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证等半径到达时尚未完成、随后自动完成、双端视口和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/012-desktop-1440.png`、`012-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否由两侧延迟鼓起推断应继续按住；物理触屏长按是否会触发浏览器系统菜单。
- **停止结论**：工程硬门已通过；不增加计时环、百分比或文字指令，进入 013。

### 013 `signal-oscillation` — 工程验收通过

- **设计事实**：上下两条纸带波形同频但错相，只有零点位置具有互补切口。上层纸带可水平移动、下层保持静止；波峰重合只是有依据的诱饵，两个零点切口进入宽松共同区才完成。
- **实现结果**：用专属双波纸带替换通用横向滑块。上层从错相位置开始，第二次向右移动时波峰重合并进入 `peak` 状态但不通关；第四次进入 68–82 的零点吸附区，自动归到 75 并完成。指针水平拖动按 2px/级换算，约 118px 的宽松拖动即可进入目标区；点击无效，下层始终静止。
- **信息层级**：未破解前不显示“零点重合”，移除随机环境图标。相同频率、错位纸带、互补零点切口和波峰诱饵承担线索，无调频滑杆、显式目标圈或步骤文字；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：35 / 35 通过，其中 3 项覆盖点击失败、波峰诱饵失败、零点吸附成功、下层静止和宽松拖动。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；首次桌面验收发现初始波带压入秒表边界约 6px，收窄容器后双端重跑通过。
  - 截图：`artifacts/screenshots/v2-sequential/013-desktop-1440.png`、`013-mobile-390.png`，已人工检查；手机端额外拉开主按钮间距后再次通过。
- **仍缺证据**：未知答案玩家是否会从互补切口推断零点优先于波峰；物理手机水平拖动时切口吸附是否自然。
- **停止结论**：工程硬门已通过；不增加“波峰错误”文字反馈或数值相位读数，进入 014。

### 014 `corner-cross` — 工程验收通过

- **设计事实**：横纵两条断裂丝带都指向同一中心压痕；拖动一条时，另一条以 35% 的位移镜像响应。只有横切口和纵切口同时进入各自 36px 中心区才自动吸附，单轴对齐只显半个十字。
- **实现结果**：用专属双轴场景替换单一横向滑块。横带右移 60px 后横轴进入目标区，纵带同步上移 21px 但仍偏 69px，因此不能通关；随后纵带上移 50px，横带同步右移 17.5px，两轴同时进入宽容区并立即完成。键盘用横带两次向右、纵带一次向上表达相同交替调整，无双指要求。
- **信息层级**：未破解前不显示“角点十字”，移除随机环境图标。断裂色带、共同压痕、35% 镜像和半十字承担反馈，不显示坐标、双轴进度或目标框；桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：38 / 38 通过，其中 3 项覆盖 35% 镜像、单轴失败、双轴 36px 吸附和键盘路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：各 1 项通过；首次浏览器运行发现交叉中心由纵带自然占据，最终测试改为真实点击可见横带臂且未使用 `force`，验证自然命中、视口与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/014-desktop-1440.png`、`014-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否理解只出现半十字意味着还需切换另一条丝带；物理手机抓取横带臂和纵带臂的顺序是否自然。
- **停止结论**：工程硬门已通过；不增加轴向箭头、坐标读数或双指引导，进入 015。

### 015 `binary-blink` — 工程验收通过

- **设计事实**：三段同心纸弧按交替朝向形成一圈，但中段纸弧与另外两段方向相反。机械地把三段依次全部翻面只会从 `010` 变成另一个错误图案 `101`；观察纸纹和断口后，只翻转反向的中段，三段才会共同闭合成 `000`。
- **实现结果**：用专属三弧翻面场景替换通用翻牌器。每段支持点击、短滑和 Enter/Space 翻面；指针抬起后的浏览器合成 click 会被抑制，确保一次动作只翻一次。三段全部翻转不会通关；恢复 `010` 后只翻中段进入 `000` 并自动完成，无提交按钮或试遍结果表。
- **信息层级**：未破解前不显示“交替环”，移除随机环境图标。纸弧朝向、断口和中段纸纹承担线索，不显示二进制提示、序号步骤或目标图案；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：41 / 41 通过，其中 3 项覆盖三段机械翻转失败、中段单独翻转成功、指针单次翻面和键盘路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证错误图案 `101` 不通关、恢复后正确图案 `000` 成功、三段位于视口内且不覆盖核心区。
  - 首次桌面验收的整页 `reload` 遇到 Next.js 开发态热更新清单瞬时解析错误；测试改为用关卡内可逆翻面恢复初始图案后双端稳定通过，未把开发服务器异常误判为玩法证据。
  - 截图：`artifacts/screenshots/v2-sequential/015-desktop-1440.png`、`015-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否从中段纸纹和断口方向识别唯一反向弧片；物理手机短滑是否比点击更有翻纸质感。
- **停止结论**：工程硬门已通过；不增加 `010` 数字、显式翻面箭头或正确图案预览，进入 016。

### 016 `status-rebound` — 工程验收通过

- **设计事实**：纸珠沿三个宽松轨位移动，半透明尾迹却留在可旋转外环上；直接抓珠会让它从指针下跳到下一处，重复追逐不会积累任何进度。玩家应改变承载尾迹的外环，让尾迹亮端落到纸珠的下一经过位，而不是继续追目标本身。
- **实现结果**：删除通用“旋转纸片点两次”控制器，改为专属纸珠、三轨位、半透明尾迹、亮端和可旋转外环。纸珠在未操作外环时每 1.2 秒平滑移向下一轨位；一旦转动外环便冻结当前预测关系，避免反应速度考验。方向键每次旋转 30°，指针可做任意圆周拖动；亮端距下一轨位 45° 内回弹收窄，15° 内自动吸附，因此完整通过区为 30°，高于 24°硬门。
- **信息层级**：未破解前不显示“追上自己的尾迹”，移除随机环境图标。珠子逃开、尾迹留在外环和接近时回弹减弱承担因果反馈，无箭头、旋转刻度、进度数字或目标文案；桌面位于秒表右侧，手机位于主按钮下方。减少动态模式取消平滑过渡，只保留三个离散轨位。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：44 / 44 通过，其中 3 项覆盖连续抓珠六次失败、接近时回弹收窄、30°宽容区、方向键路线和宽松圆周拖动。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；再次与 013–015 合并回归后双端 8 / 8 通过。
  - 截图：`artifacts/screenshots/v2-sequential/016-desktop-1440.png`、`016-mobile-390.png`，已人工检查；外环、纸珠均在视口内且不覆盖挑战、秒表或主按钮。
- **仍缺证据**：未知答案玩家能否由“珠子会逃、尾迹留在外环”推断要移动路径；物理触屏圆周拖动是否自然且不会被当成页面滚动。
- **停止结论**：工程硬门已通过；不增加预测角度、方向箭头或“移动外环”文字，进入 017。

### 017 `inverted-nibble` — 工程验收通过

- **设计事实**：四张纸片的上下半部共同形成镜像数字形，只有第三片同时出现两处异常：中央折痕朝向与其余三片相反，右上纸纤维也反向；其下半字没有遵循镜像关系。把四片机械地各翻一次只会得到 `1111`，正确结构只需翻第三片成为 `0010`。
- **实现结果**：以四张专属半字纸片替换通用单按钮翻牌。每片支持点击、上下轻扫和 Enter/Space；一次指针动作只翻一次。翻动会真实改变下半字镜像状态及纸面色泽；只有第三片翻面、其余保持正面时才贯通横向纸纹并自动完成，无提交按钮或试遍结果表。
- **信息层级**：未破解前不显示“上下半字”，移除随机环境图标。上下字形、斜折痕与纸纤维构成语言无关的多重证据，不显示异常标记、片号提示或正确图案；桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：47 / 47 通过，其中 3 项覆盖四片全翻失败、折痕/纤维双依据、第三片轻扫成功和 Enter 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证 `1111` 错误路径、恢复 `0000`、唯一 `0010` 正确路径、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/017-desktop-1440.png`、`017-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否在不依赖片号的情况下同时注意折痕和纸纤维；物理手机轻扫与点击折边的手感是否都自然。
- **停止结论**：工程硬门已通过；不增加“第三片”文字、异常高亮或镜像辅助线，进入 018。

### 018 `outer-ones` — 工程验收通过

- **设计事实**：两条细长刻痕位于固定半径的外圈上，沿外圈把它们滑到一起仍不会进入黄色内环；问题不在刻痕位置，而在“里面”的边界太小。扩大共同内环，直到它同时包住两条刻痕，才把“外侧”改写为“内侧”。
- **实现结果**：以专属双刻痕、固定外圈和可缩放内环替换通用横向滑块。两条刻痕可独立沿圆周拖动或用左右键每次转 15°，但永不改变径向位置；内环支持单指向外拖边、双指距离增强及 `+/-`/方向键。尺寸从 20 起步，70–100 为宽松完成区，键盘三次放大到 80 自动完成；不调用页面或浏览器缩放。
- **信息层级**：未破解前不显示“内外改写”，移除随机环境图标。固定外圈、黄色内环、两条断裂刻痕与边缘折把承担线索，无缩放百分比、目标圈或操作说明；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：50 / 50 通过，其中 3 项覆盖刻痕合并失败、60 尺寸失败、70–100 宽松区、单指外拖和键盘路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证刻痕角差归零仍失败、内环 60 仍失败、80 成功、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/018-desktop-1440.png`、`018-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否从“刻痕只能绕圈”推断应改变内部边界；物理手机单指拖环边与双指增强的手感是否都自然。
- **停止结论**：工程硬门已通过；不增加缩放手势图标、尺寸读数或显式包围目标，进入 019。

### 019 `double-housing-loop` — 工程验收通过

- **设计事实**：珊瑚外壳与薄荷内壳都缺一段，初始缺口完全同向重叠，因此两层自身都不完整。旋转外壳会让内壳等量反向旋转；当缺口来到相对两侧时，真正闭合的不是任一纸壳，而是两层之间原本不起眼的夹层。
- **实现结果**：以专属双壳、反向角度耦合和夹层墨环替换通用旋转按钮。拖动外壳按圆周角连续旋转，滚轮与方向键按 15°档位跳转；内壳始终使用相反角度。缺口分离低于 90°时夹层保持浅点线，90°后逐渐显墨，150°–180°的 60°宽容区内自动吸附到相对位置，无需精确保持或松手计时。
- **信息层级**：未破解前不显示“双壳闭环”，移除随机环境图标。两个同向缺口、相反转动和中间点状纸纹构成线索，无角度读数、旋转箭头或“看夹层”文字；桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：54 / 54 通过，其中 4 项覆盖缺口重叠失败、反向 15°联动、60°完成宽容带、圆周拖动和滚轮路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证初始重叠、120°夹层仅生长、180°闭合、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/019-desktop-1440.png`、`019-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否把中间浅点环理解为第三种可闭合结构；物理触屏圆周拖环与页面滚动之间是否足够稳定。
- **停止结论**：工程硬门已通过；不增加缺口对位标尺、旋转方向箭头或夹层高亮预告，进入 020。

### 020 `quiet-circuit` — 工程验收通过

- **设计事实**：三个看似按钮的墨点中心会打断线路，真正可通行的是各自外围几乎不可见的焦点环。机械点击三个中心只留下三次短促断点并清空路径；按空间顺序绕过三个中心，让连续纸纹依次穿过外围环，才形成完整静默线路。
- **实现结果**：以专属三节点静默线路替换通用焦点卡片。中心按钮从键盘 Tab 顺序中移除，点击任一中心都会增加断点并把路径归零；外围环支持按序获得焦点，也支持鼠标/触摸从第一个环连续划过第二、第三个环。命中采用节点周围 17% 的宽松半径，不要求像素级描线；顺序错误会重新从可成立的首环开始，而不是累积机械点击进度。
- **信息层级**：未破解前不显示“静默线路”，移除随机环境图标。三个深色中心承担诱饵，外围细环在靠近或获得焦点后才显墨；不显示编号、箭头、路线图、步骤数字或独立弹窗。桌面位于秒表右侧留白，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：57 / 57 通过，其中 3 项覆盖三个中心连续点击失败、外围环焦点顺序成功、宽松连续划线和断点反馈。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；随后与 017–019 合并回归，双端 8 / 8 通过。
  - 截图：`artifacts/screenshots/v2-sequential/020-desktop-1440.png`、`020-mobile-390.png`，已人工检查；三个节点均在视口内且不覆盖挑战、秒表或主按钮。
- **仍缺证据**：未知答案玩家能否从“中心会断路、外围环会显墨”推断应绕开中心；物理触屏连续划过三个外围环时是否稳定。
- **停止结论**：工程硬门已通过；不增加路线箭头、节点序号或显式“绕开按钮”提示，进入 021。

### 021 `three-beat-warmup` — 工程验收通过

- **设计事实**：纸框缓慢呼吸，三枚墨点保持固定，只有纸框的三段边缘依次经过墨点；玩家应把不随呼吸移动的墨点识别为稳定参照，在三次交会的前后 350ms 宽松窗口内依次回应。任意三次快速点击不是答案。
- **实现结果**：以专属非对称呼吸纸框、三枚固定墨点和三段独立边缘替换通用“均匀点击三次”按钮。每拍 1.8 秒，其中 700ms 为可见交会窗；正确命中会在墨点外留下永久环纹，窗外或错误顺序的点击只让对应边回弹且不清除已理解的正确拍。点击、触摸和 Space 走同一判断路径，第三拍命中后自动完成。
- **信息层级**：未破解前不显示“三拍热身”，移除随机环境图标。纸框的呼吸、固定墨点、交会压痕与命中环纹承担全部线索，无节拍数字、倒计时、进度文案或独立敲击按钮；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：60 / 60 通过，其中 3 项覆盖窗外三连点失败、固定参照、700ms 三拍顺序和 Space 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证同步窗外连点不积累、三次可见交会依序成功、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/021-desktop-1440.png`、`021-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否在两轮内把固定墨点理解为拍点参照；物理手机轻触的主观节奏感是否自然。
- **停止结论**：工程硬门已通过；不增加节拍灯、显式数字或声音依赖，进入 022。

### 022 `metronome-leak` — 工程验收通过

- **设计事实**：四条等距落水线中只有三条带墨珠，第三条空线下方的纸面却与其他三条以相同周期出现涟漪。第一轮只用于看见“没有落下也有回声”的异常；下一轮在空位涟漪前后 450ms 内触碰，才补出缺失墨滴。
- **实现结果**：以四条专属落水线、三枚同步下落墨珠、四组纸面涟漪和一枚只在交会窗显影的虚墨滴替换通用节拍按钮。每轮 2.4 秒，第一轮空位点击仍会溅开；观察满一轮后，第二轮 900ms 宽窗内选择第三条线自动补滴。点击任一已有墨珠永远只产生水花；四条线均可点击/触摸，并支持聚焦后 Space/Enter。
- **信息层级**：未破解前不显示“漏掉的一滴”，移除随机环境图标。缺失墨珠、仍然存在的虚线和涟漪构成答案依据，无空位高亮、倒计时、线号文字或弹窗；桌面位于秒表左侧，手机位于主按钮下方，与上一关的呼吸纸框保持明显场景差异。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：63 / 63 通过，其中 3 项覆盖已有墨珠点击失败、首轮空位失败、第二轮 900ms 宽窗成功和 Space 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证四次错误选择均不通关、第二轮空涟漪补滴成功、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/022-desktop-1440.png`、`022-mobile-390.png`，在错误水花结束后采集并人工检查。
- **仍缺证据**：未知答案玩家是否会等待第二轮并把空线涟漪解释为缺失墨滴；物理手机上 900ms 窗口的主观宽容度。
- **停止结论**：工程硬门已通过；不增加空位箭头、节拍数字或声音提示，进入 023。

### 023 `slow-clap` — 工程验收通过

- **设计事实**：三片声纹花瓣的宽度与纹理密度各不相同，纸带中的三段沉默空隙也有窄、中、宽三种长度，但空隙顺序是中、窄、宽。把花瓣机械地按窄、中、宽排队会得到错误图案；只有逐一匹配等长空隙才完成。
- **实现结果**：以三片可拖花瓣和一条含三段真实空隙的纸带替换通用影子排序。花瓣尺寸为宽、中、窄，空隙顺序为中、窄、宽；拖放命中区在空隙外扩 22px，放错会保留失配斜边，放对则花瓣边缘与空隙同频呼吸。键盘聚焦花瓣后可用方向键选择空隙、Enter/Space 放置；同一空隙只保留一片，允许无惩罚重排。
- **信息层级**：未破解前不显示“把花瓣放回沉默”，移除随机环境图标。尺寸、纹理间距和空隙长度提供冗余证据，无序号标签、配对连线、进度数字或现场打拍；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：66 / 66 通过，其中 3 项覆盖按大小直接排序失败、键盘正确匹配和 22px 宽松指针拖放。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；先用键盘构造错误 `0,1,2`，再用真实指针拖成正确 `1,0,2`，并验证视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/023-desktop-1440.png`、`023-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否把空隙长度理解为“沉默”并在 15 秒内完成；物理手机连续拖三片花瓣的手感。
- **停止结论**：工程硬门已通过；不增加尺寸文字、配对颜色或答案连线，进入 024。

### 024 `precision-five` — 工程验收通过

- **设计事实**：五点沿曲线排列，四段可见路程依次为短、长、短、长，墨珠假定恒速，因此路程本身就是拍谱。玩家必须先从带尾点一笔经过五点，再用五拍复现四段间隔；短拍 420ms、长拍 720ms，各自允许 ±35%。
- **边界校正**：原有 ±35% 区间使短拍 `273–567ms` 与长拍 `468–972ms` 重叠，四个 500ms 等拍会同时落入两类单段范围。为保持宽容但阻止机械等拍，增加“长拍平均值至少为短拍平均值 1.25 倍”的结构门；这不缩窄单拍窗口，也不要求毫秒级准确。
- **实现结果**：以一条带尾迹的五点折曲线、连续描线层和沿点移动的墨珠替换通用节拍按钮。指针/触摸必须在同一笔中按序经过五个 15% 宽容点；键盘用上、上、下、下表达相同曲线转折。完成描线后，点击/触摸同一区域或 Space 记录五拍；有效拍使墨珠前进，越界或等拍结构失败会让曲线回缩并清空本轮拍点。
- **信息层级**：未破解前不显示“曲线五拍”，移除随机环境图标。点间距离、恒速墨珠、带尾起点和回缩反馈构成全部证据，无 Simon 灯序、间隔数字、倒计时或声音依赖；桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：69 / 69 通过，其中 3 项覆盖未描线前敲击无效、完整指针描线、四个 500ms 等拍结构失败和键盘精确短长路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证描线前快速敲击无效、真实指针一笔经过五点、100ms 快拍失败、短长节奏成功、视口边界与核心区不遮挡。
  - 随后与 021–023 合并回归，双端 8 / 8 通过。
  - 截图：`artifacts/screenshots/v2-sequential/024-desktop-1440.png`、`024-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否把路程自然解释为节拍长度并在两次尝试内完成；物理手机描线后立即敲拍的节奏手感。
- **停止结论**：工程硬门已通过；不增加节拍标签、路线箭头或声音记忆，进入 025。

### 025 `beacon-beat` — 工程验收通过

- **设计事实**：两枚明亮灯珠同步闪烁，中间暗珠在每个 2.4 秒周期的中段才短暂鼓起。明灯是有依据的诱饵：重复点击只留下回声；真正异常是没有发光却会在两次闪烁之间呼吸的暗珠。
- **实现结果**：以专属双灯、暗珠和纸纤连接线替换通用节拍按钮。暗珠每周期仅在 800–1600ms 的 800ms 宽窗进入可破解状态，并用轮廓加粗与轻微放大提供场景内反馈；点击任一亮灯只增加回声，过早或过晚触碰暗珠只产生一次内缩，宽窗内点击、触摸或 Space 自动完成。
- **信息层级**：未破解前不显示“暗拍灯塔”，移除随机环境图标。两灯同步、暗珠位于时间中点和短暂呼吸承担全部线索，无倒计时、节拍数字、闪光入口或“点暗处”说明；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：72 / 72 通过，其中 3 项覆盖连续点击亮灯失败、暗珠窗外失败、800ms 宽窗成功与 Space 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证六次亮灯机械点击不通关、暗珠窗外不通关、可见鼓起窗口内成功、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/025-desktop-1440.png`、`025-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否在两轮内注意到同步亮灯之间的暗珠呼吸；物理手机上 800ms 窗口的主观宽容度。
- **停止结论**：工程硬门已通过；不增加暗珠高亮、提示箭头或声音依赖，进入 026。

### 026 `five-beat-divider` — 工程验收通过

- **设计事实**：五枚等距墨点被透明分隔片错误切成四加一。原稿只规定目标为二加三，却没有说明为什么三加二不是等价答案；为满足可推断硬门，在分隔片左、右边缘分别加入两道珊瑚纸痕和三道薄荷纸痕，作为解题前已存在、无文字依赖的方向证据。
- **实现结果**：以专属五点长纸带和可自由横拖的透明分隔片替换固定轨道纸片。分隔片可落在四个宽槽位，初始位于第四槽；一加四、三加二、四加一均触发轻微纸边回弹但不通关，第二槽形成二加三后左右纸纹分别以二拍和三拍密度呼吸并自动完成。方向键每次移动一槽，与指针路线语义等价。
- **信息层级**：未破解前不显示“二加三”，移除随机环境图标。错误的四加一轮廓、五个等距点和分隔片两侧 2/3 道纸痕承担线索，无数字标签、目标框、拍点演奏或答案文字；桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：75 / 75 通过，其中 3 项覆盖一加四、三加二、四加一失败，2/3 纸痕前置证据，方向键成功和宽槽指针放置。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证三种错误分组均不误过、真实拖动到第二槽成功、视口边界与核心区不遮挡。
  - 首次桌面自动化连续拖动在 140ms CSS 位移动画尚未结束时抓空已移动的透明片；测试改为等待 180ms 视觉过渡结束后再执行下一次真实拖动，双端稳定通过。玩法判断与宽槽边界仍由组件测试独立覆盖。
  - 截图：`artifacts/screenshots/v2-sequential/026-desktop-1440.png`、`026-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否把左右边缘纸痕自然理解为分组依据；物理手机拖动透明片时纸痕是否足够可见。
- **停止结论**：工程硬门已通过；不增加 2/3 数字、箭头或显式分组说明，进入 027。

### 027 `beacon-metronome` — 工程验收通过

- **设计事实**：恒速光束沿圆轨扫描四枚纸点，三枚位于 0°、90°、180°，迟到点位于 300°，因此前后两段分别为明显不等的 120° 与 60°。答案不是卡住某次闪光，而是把迟到点移回四段都为 90° 的空间关系。
- **实现结果**：以专属圆轨、恒速光束、三枚固定纸点和一枚可沿环拖动的迟到点替换通用横向纸片。指针按圆心角连续映射，方向键每次移动 10°；目标中心为 270°，前后相邻距离各允许 ±12%，合计 21.6° 完成带，进入后自动吸附为四个等距四分点。每次移动都从当前位置重放光束，不要求玩家凭毫秒判断。
- **信息层级**：未破解前不显示“把迟到变成距离”，移除随机环境图标。恒速光束、明显不等的两段弧长和同形纸点承担线索，无角度数字、目标刻线、倒计时或“移动绿点”说明；桌面位于秒表右侧，手机位于主按钮下方。减少动态模式改为四条离散光束位置。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：78 / 78 通过，其中 3 项覆盖单击失败、120°/60° 前置异常、方向键等距成功、12% 完成带边界和圆轨指针路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证普通点击与错误角度不通关、真实圆轨拖动到宽容区成功、视口边界与核心区不遮挡。
  - 首轮浏览器测试发现互动纸点自身的持续缩放动画会让自动化等待元素稳定，也会降低快速抓取的可靠性；动画已移到不参与命中的外圈伪元素，真实按钮几何保持稳定后双端通过。
  - 截图：`artifacts/screenshots/v2-sequential/027-desktop-1440.png`、`027-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否在一次光束循环内把迟到理解为空间距离异常；物理触屏沿圆环拖动的手感。
- **停止结论**：工程硬门已通过；不增加角度尺、目标缺口或闪光倒计时，进入 028。

### 028 `fourfold-ack` — 工程验收通过

- **设计事实**：四片纸瓣中三片以 1.6 秒同相开合，第三片始终错开半周期，在其余三片收拢时张开。破解不是把四片调成一样，而是识别反拍片并把它冻结，让剩余三片完整走完一个同步周期。
- **实现结果**：以专属四瓣纸花、相位动画和稳定中心替换通用长按圆点。四个按钮的命中盒保持静止，开合只发生在内部纸瓣；短点任一片都会立即释放并清零，长按同相片会让其余节奏分裂，长按反拍片 1.6 秒则中心按八级逐渐稳定并自动完成，玩家无需精准松手。Space 与指针长按走同一计时路线。
- **信息层级**：未破解前不显示“留下那片反拍”，移除随机环境图标。三片同相开合、一片半周期反相以及中心由乱到稳承担全部线索，无异常箭头、花瓣编号、进度数字或“长按绿片”说明；桌面位于秒表左侧，手机位于主按钮下方。形状开合承担主信息，薄荷色只作冗余而非唯一依据。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：81 / 81 通过，其中 3 项覆盖四片快速遍历失败、同相长按扰乱、反拍整周期成功、八级稳定反馈和 Space 不精确松手路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证四次机械点击不误过、真实长按同相片 1.75 秒仍失败、真实长按反拍片自动成功、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/028-desktop-1440.png`、`028-mobile-390.png`，已人工检查。
- **仍缺证据**：未知答案玩家能否在一至两轮动画内识别反拍片；物理触屏长按是否会被系统手势干扰。
- **停止结论**：工程硬门已通过；不增加相位标记、计时环或文字指令，进入 029。

### 029 `pulse-checker` — 工程验收通过

- **设计事实**：九个等距位置中缺少第六道脉冲刻痕，肉眼只能看见一列近似等距线；透明检查尺拥有完整九道刻线。必须先让两组刻线重叠并发生视觉消隐，唯一没有对应物的尺刻线才成为可依据的亮槽。
- **实现结果**：以专属脉冲纸带、透明检查尺、缺口槽和备用墨点替换通用共享纸片。第一步把检查尺二维拖到纸带宽覆盖区，八组重合刻线降至 8% 透明度，第六道尺线变为薄荷亮槽；第二步把备用点拖入横向 ±8%、纵向 ±15% 的宽松区域。检查前碰巧投中真实位置、检查后投到其他位置都回弹到起点并失败；键盘先下移尺，再以方向键移动墨点，语义等价。
- **信息层级**：未破解前不显示“缺口检查尺”，移除随机环境图标。两组等距刻线、重叠后的消隐和唯一亮槽承担全部线索，无槽位编号、箭头、步骤卡或答案文字；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：84 / 84 通过，其中 3 项覆盖检查前幸运投中失败、尺子真实改变信息、非亮槽失败、宽松正确放置和键盘两步路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证两类错误投放、真实尺子覆盖、消隐状态、真实正确拖放、视口边界与核心区不遮挡。
  - 首轮截图目检发现错误投放的珊瑚回弹环在动画结束后恢复常显；已将结束态固定为透明并重跑双端验收与截图。
  - 截图：`artifacts/screenshots/v2-sequential/029-desktop-1440.png`、`029-mobile-390.png`，均在检查尺揭示亮槽后采集并人工检查。
- **仍缺证据**：未知答案玩家能否理解“尺刻线消隐”是比较结果而非装饰动画；物理手机连续两次拖放的手感。
- **停止结论**：工程硬门已通过；不增加位置编号、显式目标框或步骤文案，进入 030。

### 030 `broken-measure` — 工程验收通过

- **设计事实**：纸谱必须同时让玩家看出“四拍”和“只出现三条主线”。原稿只写纸谱标着四个等宽小节，没有规定视觉证据；实现用顶部四个等距压痕表达四拍数量，正面谱纸只保留三条长墨线，右下角则以双层纸边和不同厚度构成唯一异常。第四条薄荷墨线确实印在折角背面，半掀才出现影子。
- **实现结果**：以专属谱纸、四拍压痕、三条正面墨线和可掀右下折角替换通用翻片。普通点击只累计触碰而不改变纸张，连续四次也不能通关；向右或向下翻会产生纸边回弹。向左上拖动时折角按二维位移连续掀起，进度达到 35% 后露出背面线影，横向至少 35px 且纵向至少 28px 后自动翻平并把第四线接入谱面。Enter/Space 提供不依赖精细指针的等价翻页路线。
- **信息层级**：未破解前不显示“藏在折缝里的第四拍”，移除随机环境图标。四个压痕与三条主线形成可数差异，厚折角承担发现线索；无箭头、折页手势图、步骤数字或答案文字。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：88 / 88 通过，其中 4 项覆盖四次点击失败、错误方向回弹、半掀线影、二维宽阈值成功和 Enter 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实四次点击、错误方向拖动、半掀截图状态、真实左上折页成功、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/030-desktop-1440.png`、`030-mobile-390.png`，均在第四线刚从折角背面显影时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把四个压痕与三条长线自然理解为缺失一拍；物理手机斜向掀角是否顺手。
- **停止结论**：工程硬门已通过；不增加“向左上拖”文案、手势箭头或第四线目标位，进入 031。

### 031 `escape-hatch` — 工程验收通过

- **设计事实**：固定纸圆直径明显大于初始内窗，左右两端被同一扇小窗裁掉；窗框右边缘具有三段细纸痕和独立厚度，圆环则没有目标槽或可移动终点。因而可先通过拖圆确认“怎么移动都仍被裁”，再把注意力转向能改变可见范围的窗框。
- **实现结果**：以专属裁切窗、固定大圆、可试拖圆层和可调右窗框替换通用横向纸片。拖圆超过 12px 只产生有限位移并回到中心，永不通关；真实 `resize` 事件没有接入解题状态。窗框从 44% 宽开始，可在 36%–78% 连续调节；缩窄会触发窗边回弹，达到 60% 时两端纸纹开始相接，达到 68% 的宽松阈值后自动吸附并闭合整圆。方向键以 8% 宽步长提供等价路线。
- **信息层级**：未破解前不显示“比窗口更大的圆”，移除随机环境图标。被裁两端、可短暂移动但仍不完整的圆和有纹理的厚窗边构成因果线索，无 resize 图标、横向箭头、百分比数字或“拖窗框”文案；桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：92 / 92 通过，其中 4 项覆盖浏览器 resize 无效、拖圆回位、缩窄回弹、宽阈值闭合和键盘路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实拖圆失败、页面 resize 事件失败、真实窗框缩窄失败、62% 近接状态、70% 拖动成功、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/031-desktop-1440.png`、`031-mobile-390.png`，均在圆环两端刚接近闭合、窗框仍可辨识时采集并人工检查。
- **仍缺证据**：未知答案玩家能否在试拖圆后自然转向窗框；物理手机窄屏上右窗边的抓取手感。
- **停止结论**：工程硬门已通过；不增加缩放图标、方向箭头或显式“框”提示，进入 032。

### 032 `landscape-nudge` — 工程验收通过

- **设计事实**：前半标题固定在横向浅黄纸条，后半标题固定在与它共享折痕的薄荷纸条上，但初始纸条沿 90° 方向延伸。文字本身只形成 `TIME / BENDS`（中文为“时间 / 会弯”）的可读短句，不写“横屏”或操作答案；两条纸边共点才是需要观察的结构证据。
- **实现结果**：以专属双标题纸条、共享折痕和隐藏铰链替换通用点击翻片。交互按钮命中盒保持静止，只有内部纸条随指针相对铰链的真实角度连续旋转；移动超过 6px 才显出 90° 铰链，普通点击连续四次均无效。纸条进入 ±35° 时字距开始校正，但只有进入 ±15° 宽完成带才自动吸附到 0° 并接成同一基线。Enter/Space 直接完成铰链翻转，方向键以 15° 步长提供等价路线；设备方向事件不参与判定。
- **信息层级**：未破解前不显示“横过来的标题”，移除随机环境图标。横竖纸条、共享折痕和移动后才显现的铰链承担线索，无旋转箭头、角度数字、横屏图标或答案文案；桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：96 / 96 通过，其中 4 项覆盖方向事件无效、四次点击无效、铰链显影、斜向失败、宽水平带成功和 Enter 路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；首轮真实点击暴露抬起点被误算为铰链中心 0° 的缺陷，已用 12px 拖动门槛与合成 click 抑制修复后验证四次点击失败、斜向失败和真实水平旋转成功。
  - 025–032 双端批次回归：16 / 16 通过。
  - 截图：`artifacts/screenshots/v2-sequential/032-desktop-1440.png`、`032-mobile-390.png`，均在斜向接近且铰链显影时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把共享折痕识别成可旋转铰链；物理手机斜向转纸手感以及中文短句的自然度。
- **停止结论**：工程硬门已通过；不增加转向箭头、角度刻度或“横过来”提示，进入 033。

### 033 `window-tilt` — 工程验收通过

- **设计事实**：窗框随整张场景纸保持水平，因此初看并无异常；不属于纸面的铅坠却稳定偏向一侧，纸内横纹也有轻微斜率。真正需要校正的是场景纸与固定重力线的关系，而不是拖动铅坠、点击窗框或旋转设备。
- **实现结果**：以专属整页窗纸、双线窗框、中心缺口、斜纸纹和世界坐标铅坠替换通用旋转图标。指针横向拖动连续改变整张纸的角度，反向旋转会让铅坠偏移由 24 增至 44，并触发纸框回弹；正确方向会逐渐缩短偏移，进入目标 `-12° ±6°` 的宽松完成带后自动吸附，让固定铅垂线穿过中心缺口。普通点击只记录触碰，`deviceorientation` 事件不接入判定；方向键以 4° 步长提供语义等价路线。
- **信息层级**：未破解前不显示“窗口倾斜”，移除随机环境图标。窗框与纸一起保持自洽、铅坠相对纸面偏移、斜纸纹和中心缺口承担全部线索；无旋转箭头、水平仪数字、角度刻度或答案文案。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：100 / 100 通过，其中 4 项覆盖设备方向无效、普通点击无效、反向偏移加剧、正确方向吸附、六度宽容带和键盘路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证点击与设备方向事件不误过、真实反向拖动进入 `worse`、再向正确方向移动进入 `closer`、最终落入宽容带成功，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/033-desktop-1440.png`、`033-mobile-390.png`，均在纸张已向正确方向转动、铅坠偏移缩小但尚未自动吸附时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把“窗框看似水平、铅坠却偏斜”的矛盾理解为应旋转整张场景纸；物理手机横向转纸的手感。
- **停止结论**：工程硬门已通过；不增加水平线、方向箭头、角度数字或设备倾斜提示，进入 034。

### 034 `double-horizon` — 工程验收通过

- **设计事实**：两张半透明风景纸分别只有天空和倒影，靠近地平线的一边都以同一组不对称撕口收尾；它们各自不完整，且倒影纸初始转过 90°。移动任何一张会让另一张的同形撕口从淡痕显色，但单纯把两张纸堆在一起仍不能生成地平线。
- **实现结果**：以专属天空纸、倒影纸、双太阳、同形撕口和脱离裁切层的真实撕口转轴替换通用三层堆叠。两张纸都可二维自由拖动；倒影纸的撕口圆轴可连续翻转 0°–90°，并随纸张位置与角度移动。位置横纵各有 10% 宽松吸附区，角度有 ±12° 完成带；只有空间重叠与方向同时成立才吸附为共享地平线。90° 随意重叠会明确回弹并累积错误，普通点击不改变状态；键盘以 Enter 翻面、方向键 12% 步长完成同一关系。
- **信息层级**：未破解前不显示“互相借景”，移除随机环境图标。天空/倒影材质、上下互映太阳、同形撕口和纸边转轴承担线索；无四宫格、旋转箭头、目标框、角度数字或步骤文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：105 / 105 通过，其中 5 项覆盖双纸结构、移动显色、90° 随意重叠失败、转轴翻正后宽松吸附和键盘分步路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证普通点击无效、移动天空纸揭示撕口、真实拖动倒影纸错误重叠、真实转轴分两段翻正、共享线完成、视口边界与核心区不遮挡。
  - 首轮浏览器验收发现转轴仍被旋转纸层的裁切区与 120ms 位移动画影响命中；已把转轴提升为随纸景运动的独立顶层物件，并在位置过渡稳定后抓取，双端真实指针路径随即稳定通过。
  - 截图：`artifacts/screenshots/v2-sequential/034-desktop-1440.png`、`034-mobile-390.png`，均在两景已经重叠、太阳开始互映且倒影纸仍差 20° 时采集并人工检查。
- **仍缺证据**：未知答案玩家是否会把显色的同形撕口理解为共享边关系；物理手机上先拖景再抓转轴的手感。
- **停止结论**：工程硬门已通过；不增加翻转箭头、共享线目标槽或“两步”说明，进入 035。

### 035 `horizon-shift` — 工程验收通过

- **设计事实**：初看只有一片连续天空、两条高度接近的地平线和一枚太阳；横向移动整片天空视角至少 20px 后，前景、远景与太阳才以三种速度错开，并显出第二段倒影。破解不是移动某个图标，而是继续纵向改变观察高度，让太阳与两条地平线共同进入视口高度 2% 的宽松水平带。
- **实现结果**：以专属全页三景深天空、前后地平线、太阳和两段倒影替换通用共享控件。交互面是右侧或主按钮下方的一块淡纸质天空窗口，支持连续二维拖景；横向位移分别按 0.55、0.22、0.85 的速率作用于前景、远景和太阳。只对齐任意两层会出现断开的珊瑚色倒影并保持未解锁，三层在 `y=20` 附近共同进入 2% 高度带才自动吸附为一条高度。普通点击、滚轮和 `deviceorientation` 事件均不改变解题状态；方向键提供无需权限的离散等价路线。
- **信息层级**：未破解前不显示“地平线偏移”，移除随机环境图标。初态的一段倒影、拖景后出现的第二段倒影、三层不同水平速度以及只对齐两层时的断裂共同承担线索；无方向箭头、百分比、目标槽、设备倾斜提示或操作步骤。全页天空位于核心游戏层之后，交互窗口在桌面右侧、手机主按钮下方，不覆盖挑战文案、秒表或主按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：110 / 110 通过，其中 5 项覆盖初态三层数据、20px 视差发现、只对齐两层失败、三层宽松带成功和键盘路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证普通点击、方向事件和滚轮均不误过，真实横向拖景显出三种速度，纵向 12 单位只形成不完整倒影，再移动至 20 单位才完成，同时检查视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/035-desktop-1440.png`、`035-mobile-390.png`，均在仅两层接近、第二段倒影仍断开的状态采集并人工检查。
- **仍缺证据**：未知答案玩家能否从“横移后速度不同、纵移时倒影逐渐接合”自然推断三层同高；物理手机整片天空拖动是否与页面滚动手势同样稳定。
- **停止结论**：工程硬门已通过；不增加拖动箭头、设备倾斜按钮、三层标注或目标高度线，进入 036。

### 036 `portable-horizon` — 工程验收通过

- **设计事实**：三条不同材质与颜色的纸带都各自画着看似水平的中心线；一把可搬动的纸质水平仪在三处给出不同读数。只有第三处同时让气泡居中、仪器两端投影等长；单看纸带颜色或位置没有答案依据，必须先用同一工具完成比较，再把有双重证据的纸带接到秒表基线。
- **实现结果**：以专属三纸带工作台、纸质水平仪、气泡管、双端投影和秒表基线接口替换通用横向纸片。水平仪可在整个工作台二维移动，经过三带时即时给出 `-16 / +14 / 0` 的气泡偏移与 `36:20 / 18:35 / 28:28` 的投影关系；必须比较完三处才允许第二步。未比较就幸运拖中第三带会回弹，比较后拖错带仍回弹；只有把双证据纸带放进基线接口才连接完成。键盘以水平仪上下逐带比较、再聚焦纸带按 Enter 的两段动作保持语义等价。
- **信息层级**：未破解前不显示“可以搬走的水平线”，移除随机环境图标。三条同样可信的纸面基线、同一工具在不同环境中的气泡与投影变化、右侧纸质接口承担全部线索；无正确颜色高亮、纸带编号文字、拖动箭头、步骤卡或读数数字。桌面工作台位于秒表左侧，手机重排到主按钮下方，均不覆盖核心内容。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：115 / 115 通过，其中 5 项覆盖三带结构、气泡与影长双证据、未比较猜中失败、已比较错带失败、正确接续和键盘两段路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实幸运猜带失败、水平仪逐带真实拖动与即时读数、错误纸带真实回弹、正确纸带接入基线、视口边界与核心区不遮挡。
  - 首轮浏览器验收暴露水平仪位置过渡会让快速连续抓取失焦，且水平仪覆盖正确纸带中心时会截获第二步指针；已取消工具位置滞后，并从纸带外侧宽抓取区执行搬运，双端真实路径随后稳定通过。
  - 截图：`artifacts/screenshots/v2-sequential/036-desktop-1440.png`、`036-mobile-390.png`，均在三处已比较、水平仪停在气泡居中且双影等长的纸带上采集并人工检查。
- **仍缺证据**：未知答案玩家是否会主动把同一把水平仪带过三处，并把气泡与影长理解为互相校验；物理手机上从纸带外侧抓取第二步的手感。
- **停止结论**：工程硬门已通过；不增加正确带高亮、读数文字、编号、箭头或显式“两步”提示，进入 037。

### 037 `parallax-window` — 工程验收通过

- **设计事实**：两个圆形纸窗各自只让玩家看见钟的一半：暖黄前景窗里只有钟针和轴心，薄荷后景窗里只有钟圈与刻度；单独都不成钟。拖动任一窗时，窗内图层以不同于纸窗的速度移动，证明它们看向不同景深；真正的关系是让钟针轴心与钟圈中心共享一点，而非让两个圆边缘接触。
- **实现结果**：以专属双圆窗、分层钟针、钟圈刻度、双景深位移和断续纸框替换通用三层堆叠。两窗均支持自由二维拖动；钟针景按窗口位移的 `-0.17` 倍、钟圈景按 `-0.38` 倍产生相对运动。两圆边缘接触或部分重叠只让框纹变成珊瑚色断线并记录失败，中心距离进入 36px 宽吸附区后才共同吸附到中点、框纹闭合并合成完整钟面。方向键按 24px / 20px 宽步长移动，无像素级要求。
- **信息层级**：未破解前不显示“两个窗口的一只钟”，移除随机环境图标。只有半钟内容、移动时不同景深、两张纸框的互补断纹和中心合成后的钟面承担线索；无目标圆、对齐十字、距离数字、箭头或操作说明。桌面场景位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：121 / 121 通过，其中 6 项覆盖双窗结构、真实移动才显视差、边缘接触失败、36px 中心吸附、任一窗均可完成和方向键路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证普通点击无效、真实拖窗显出前后景速度差、圆边接触不通关、继续拖入中心吸附区成功，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/037-desktop-1440.png`、`037-mobile-390.png`，均在两圆边缘已接触、钟轴与钟圈仍错位、断框反馈出现时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从两种相对移动速度理解“不同景深仍可合成一个钟”；物理手机上重叠圆窗时顶层命中是否自然。
- **停止结论**：工程硬门已通过；不增加中心标记、对齐线、距离显示或显式“重叠”提示，进入 038。

### 038 `return-ticket` — 工程验收通过

- **设计事实**：同一张回程票的左右半张分别贴在页面两侧，撕口纸纤维相同，中央没有接缝、插槽或可拼目标。向中央拖只是遵循常规拼图直觉，必须失败；真正的空间规则是页面左右边缘相连，把左半继续拖出页面后，它会从右边缘连续出现并与右半接合。
- **实现结果**：以专属左右票根、互补锯齿撕口、跨边缘复制层和不断裂的上下纸纤维替换通用横向纸片。向右拖超过 20px 会触发左侧珊瑚回弹并保持进度为零；向左拖则按宽松比例连续减少左侧可见量、同步增加右侧进入量，单次约 40px 手势即可从 `0 → 70 → 140` 完成跨边。中途不释放也能观察两边同步变化，达到 140 后才在右侧闭合撕缝。方向键每次推进 30 单位；组件不监听或调用历史、哈希、导航 API。
- **信息层级**：未破解前不显示“穿过页面的回程票”，移除随机环境图标。两边相同撕口、同一票面纹理、拖出时两侧同步的纤维和右侧逐渐出现的票面承担线索；中央始终没有目标框、箭头、进度数字或“拖出页面”文案。票根固定在桌面和手机两侧安全区，不覆盖标题、秒表或主按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：127 / 127 通过，其中 6 项覆盖对边结构、向中央失败、左右进出量同步、右侧闭缝、浏览器历史事件隔离和方向键路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实向中央拖动失败、同一指针不释放时跨边进度与纸纤维连续、继续拖动接合、URL 全程不变，以及两侧票根视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/038-desktop-1440.png`、`038-mobile-390.png`，均在左半大部分离场、同纹票面从右侧出现且纤维保持连续时采集并人工检查。
- **仍缺证据**：未知答案玩家是否会在中央拼合失败后敢于把物件继续拖出页面；真实触屏浏览器边缘返回手势是否会与左拖竞争。
- **停止结论**：工程硬门已通过；不增加环绕箭头、中央叉号、跨边文字或进度提示，进入 039。

### 039 `tab-doubleback` — 工程验收通过

- **设计事实**：同一条织纹丝带先从第一枚页签正面经过、再在第二枚页签背面消失，构成明显但不带文字的层级矛盾。第一枚页签的下沿保留背穿压痕，第二枚页签的上沿保留前穿压痕；答案必须读取这两处相反的纸面证据，而不是记一段固定方向滑动。
- **实现结果**：以专属双页签、连续织纹丝带、上下压痕、可拖活动端和保留折痕替换通用图层纸片。活动端靠近页签时根据落点位于中线上方或下方选择前层/后层；中线模糊区、错误页签、重复同一层级都会在 150ms 内回弹。只有先从第一枚下沿穿到背面、再从第二枚上沿绕到正面才闭合丝带；完成带按页签高度给出宽松区域，不要求精确描线。键盘用上/下选择前后层、Enter 穿过当前页签，保持同一层级推理。
- **信息层级**：未破解前不显示“藏在页签后的丝带”，移除随机环境图标。丝带初始遮挡关系、两处相反压痕和正确后保留的折痕承担全部线索；无前/后文字、方向箭头、步骤数字或目标框。桌面场景位于秒表左侧，手机重排到主按钮下方，均不覆盖核心内容。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：133 / 133 通过，其中 6 项覆盖初始前后遮挡、点击无效、中线固定滑动失败、第一处背穿保留、第二处重复背穿失败、前穿闭合和键盘层级路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击、中线拖动和重复背层均不误过，真实“背 → 前”拖动闭合，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/039-desktop-1440.png`、`039-mobile-390.png`，均在第一处背穿折痕已保留、第二处仍待判断时采集并人工检查。
- **仍缺证据**：未知答案玩家能否只凭两处上下压痕推断相反穿层顺序；物理触屏将丝带端放到页签上/下半区的手感。
- **停止结论**：工程硬门已通过；不增加“前/后”标签、穿线箭头或步骤提示，进入 040。

### 040 `tab-return` — 工程验收通过

- **设计事实**：右侧纸页只露出半张回转票，直接点击没有反应，向页面内抓取时票根反而继续躲向边缘；向外送出或停留后才露出与纸页压痕同形的标签切口。发现后，玩家必须让场景真正经历一次“不可见再返回”的状态变化，而不是在当前可见状态反复点票根。
- **实现结果**：以专属边缘半票、标签切口、回票压痕、整页折角和可连续覆盖的纸面替换通用盖页按钮。普通点击无效，向内拉记录一次躲避并复位；向外滑动 24px 或停留 700ms 会露出切口。发现后 30 秒内的真实 `hidden → visible` 可让票根完整返回；初次可见事件不计数，发现前切页只加深压痕，超时返回会保留发现但不通关。等价正式路线允许把折页宽松拖到 82% 以上完全盖住，再向回揭到 18% 以下完成；键盘 Enter 以同样的盖住/揭开两态工作。
- **信息层级**：未破解前不显示“标签返回”，移除随机环境图标。边缘只露半票、抓取时反向躲避、露出的标签形切口和同形压痕承担线索；无切标签图标、倒计时、步骤卡或“离开页面”说明。桌面场景位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：139 / 139 通过，其中 6 项覆盖初始 visibility 隔离、点击/向内抓取失败、向外发现、发现前与发现后切页差异、30 秒超时和盖页键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击、向内拖动、向外揭示、不完整盖页、完整盖住/揭开和视口边界与核心区不遮挡。
  - 首轮桌面验收发现折页翻到左侧后，三角裁切让可见折角中心落在计时区的上层命中范围外；已将裁切限制在内部纸片、把透明命中盒移入场景安全区并增加顶部命中断言，双端复测稳定通过。
  - 截图：`artifacts/screenshots/v2-sequential/040-desktop-1440.png`、`040-mobile-390.png`，均在票根向外显出标签切口、折页仍打开时采集并人工检查。
- **仍缺证据**：未知答案玩家会先尝试向外送票还是等待；真实浏览器/系统切标签时不同平台是否稳定发送 `visibilitychange`。盖页路线已保证这些外部差异不会阻塞通关。
- **停止结论**：工程硬门已通过；不增加标签页图标、倒计时或显式离开/返回文案，进入 041。

### 041 `help-loop` — 工程验收通过

- **设计事实**：问号纸形的内部与下方圆点都像常规可点击目标，但真正连续的纸轨位于问号外缘；内侧没有可累积的机关。玩家应从点击只产生普通涟漪、外缘却能保留拖影的差异，推断答案在轮廓外部。
- **实现结果**：以专属问号纸轨、安静圆点、内部伪目标、外缘留痕和尾端连接替换通用横向轨道。圆点或内侧点击只产生涟漪并清除路径；指针可从外缘任意节点开始，顺时针或逆时针沿 28% 宽松通道走完闭环，进入内侧会立即淡去。判定基于相邻外缘区域与拓扑顺序，不按像素轨迹模板比较；键盘左右键移动一枚可见纸珠沿同一外缘前后行走，而非输入方向密码。
- **信息层级**：未破解前不显示“问号外的答案”，移除随机环境图标。问号内外的材质差、点击涟漪、外缘保留的薄荷细线和闭环后连接圆点承担线索；无外圈箭头、路线编号、进度数字或“沿这里走”文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：145 / 145 通过，其中 6 项覆盖内外结构、问号/圆点点击失败、切入内侧淡去、任意起点双方向宽松闭环和可见键盘纸珠路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实内部点击、圆点涟漪、真实外缘连续拖影与闭环，以及视口边界与核心区不遮挡。
  - 首轮手机验收发现谜题外接矩形与主按钮存在不足 1px 的边界相交；已把移动端场景下移 1rem，而不是放宽零遮挡断言。首次截图还暴露 SVG 指针聚焦的浏览器黑框，已移除指针默认框并保留键盘 `focus-visible` 轮廓，双端复测与目检通过。
  - 截图：`artifacts/screenshots/v2-sequential/041-desktop-1440.png`、`041-mobile-390.png`，均在外缘完成约四成、薄荷拖影已保留但尚未连接圆点时采集并人工检查。
- **仍缺证据**：未知答案玩家是否会把“内部点击只有涟漪”与外缘浅轨联系起来；物理触屏沿问号弯曲外缘连续移动的手感。
- **停止结论**：工程硬门已通过；不增加外圈箭头、完成百分比或显式“外侧”标签，进入 042。

### 042 `panel-ping` — 工程验收通过

- **设计事实**：磨砂侧纸遮住了纸后关系，固定彩边只露出窄条；轻触磨砂纸任意位置会在横纵两轴的相反位置短暂回应一枚回声。连续点击只能替换上一枚回声，不能积累进度；玩家必须把整张磨砂纸二维移动，让回声回到真实彩边来源。
- **实现结果**：以专属固定彩边、磨砂纸纹、清透纸边、单枚镜像回声和二维纸层移动替换通用三纸堆叠。触点按纸面局部坐标计算，回声严格映射为 `(100-x, 100-y)`；每次轻触覆盖前一枚回声，错误移动会整纸回弹。回声进入彩边横向 6%、纵向 8% 的宽松带才显出完整彩边并破解；键盘 Enter 先探测，方向键以 8% 步长搬动同一纸层。
- **信息层级**：未破解前不显示“玻璃后的回声”，移除随机环境图标。磨砂与清透边缘的材质差、窄彩边、触点反侧出现的短回声承担线索；无镜像箭头、坐标、目标框、次数或操作文案。桌面位于秒表右侧，手机位于主按钮下方，均不覆盖核心内容。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：151 / 151 通过，其中 6 项覆盖磨砂/彩边结构、双轴镜像、连点不累积、无探测与错误移动回弹、二维回源成功和键盘路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证浏览器真实触点的双轴反射、三次轻触仍只有单枚回声、错误二维拖动回弹、按实际回声坐标回源成功，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/042-desktop-1440.png`、`042-mobile-390.png`，均在磨砂纸已产生单枚反向回声、尚未移回彩边时采集并人工检查。
- **仍缺证据**：未知答案玩家能否由“触点与回声处于纸面相反位置”推断要移动整张磨砂纸；物理手机二维拖纸时与页面边缘手势的协调感。
- **停止结论**：工程硬门已通过；不增加镜像轴、回源箭头、坐标或点击次数，进入 043。

### 043 `archive-route` — 工程验收通过

- **设计事实**：三枚没有常驻连线的档案页签以纸边、切口和浅印暗示起点、中继与封口；页签内容不是机关。只有注意力停在当前正确页签时，它才向下一枚投出半透明光带，且下一次聚焦必须在 1.7 秒连续窗口内跟上。
- **实现结果**：以专属三页签纸场、焦点柔光、两段即时光带、错误死端和终点纸封替换通用三按钮。点击“打开”页签会合页、清空路线并累计死端，三次打开也不通关；桌面悬停 220ms、手机从空白按住后连续拖过三枚光晕、键盘依次聚焦都表达同一注意力转移。跳过中继或停顿过久都会熄灭既有光带，而不会把点击次数当作进度。
- **信息层级**：未破解前不显示“注意力路线”，移除随机环境图标。页签的起始圆印、中继双切口、封口双圈以及随焦点出现的光带承担线索；无固定连线、序号文字、方向箭头、步骤卡或“不要点击”文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：157 / 157 通过，其中 6 项覆盖三页签结构、依次打开失败、连续焦点光带、跳页死端、手机按住移动和超时断路。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实 DOM 依次打开不通关、错误焦点熄灭路线、桌面真实悬停路线、手机真实按住拖过光晕，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/043-desktop-1440.png`、`043-mobile-390.png`，均在两段注意力光带已经显影、终点尚未聚焦时采集并人工检查。
- **仍缺证据**：未知答案玩家是否会从“页签被打开会熄灭、停留却投出光带”理解为移动注意力；物理手机按住空白再穿过纸片时是否会误触系统滚动。
- **停止结论**：工程硬门已通过；不增加常驻路线、页签编号、箭头或显式“不点击”提示，进入 044。

### 044 `focus-orbit` — 工程验收通过

- **设计事实**：一组可见的小数点轨道由三枚错位彩色残影构成，轨道本身是显眼的常规目标；独立透明焦点片位于纸面另一侧。点击轨道只应产生涟漪，只有移动焦点片超过 16px 才能看见局部线条收清，并把三层残影带回同一中心。
- **实现结果**：以专属三残影轨道、三层纸环、透明焦点片、局部清晰度和 44px 真实吸附区替换通用焦点按钮。轨道连续点击只记录涟漪且不移动焦点片；小于 16px 的轻触抖动也不进入发现。真实二维拖动后焦点片跟手移动，远处释放保留清晰线索但不通关，进入可见轨道宽容区才吸附到 `74,38` 并把三色小数点合为薄荷单点；方向键以可见 14% / 12% 步长表达同一移动。
- **信息层级**：未破解前不显示“焦点轨道”，移除随机环境图标。错位的三色点、不同直径纸环、透明片内的十字折纹以及拖动后变清的边缘承担线索；无目标热区、方向箭头、距离数字或步骤文案。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：163 / 163 通过，其中 6 项覆盖显式三残影结构、轨道连点失败、16px 门槛、远处释放、44px 吸附和键盘移动。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实轨道四次点击、10px 轻推、远处真实拖动显清、再拖入轨道中心吸附，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/044-desktop-1440.png`、`044-mobile-390.png`，均在焦点片已移动并显清局部、三层残影尚未对齐时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把透明片理解为观察位置而非另一枚按钮；物理手机上透明片在浅色背景中的可抓取感。
- **停止结论**：工程硬门已通过；不增加目标框、移动箭头、距离显示或“拖焦点片”提示，进入 045。

### 045 `focus-cascade` — 工程验收通过

- **设计事实**：四张半透明描图纸以不等偏移真实叠放，每层只有一段模糊的 `10.00` 字形；纸边切口、透明度与投影共同给出前后深度。焦点片覆盖某层时该层局部变清、下一层反而更模糊，答案是按最深到最前的关系传递清晰度，而非按屏幕左右扫过。
- **实现结果**：以专属四层描图纸、深浅纸边、数字碎片、同一枚可移动焦点片和保留式清晰度传递替换通用焦点按钮。每次真实拖动会预览当前清晰、下一层加深；正确顺序 `deepest → lower-middle → upper-middle → front` 每层只需一次宽松落点。错误选择把清晰度送回但保留已经理解并点亮的深层，不全盘清零；机械按横坐标顺序会出现两次回送且不能误过。键盘左右移动可见焦点片，Enter 选择当前层，全程不显示步骤编号。
- **信息层级**：未破解前不显示“焦点瀑布”，移除随机环境图标。四纸层的真实遮挡、透明度、右侧切口高度、模糊数字与焦点片局部清晰承担线索；无层号、深浅文字、顺序箭头、目标框或步骤卡。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：169 / 169 通过，其中 6 项覆盖四层深度结构、直接点击与左右机械顺序失败、局部预览、错误不清零、四步正确传递和键盘选层。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证四层直接点击不计移动、真实左右机械路径产生两次回送、已清晰深层保留、再按关系完成，以及视口边界与核心区不遮挡。
  - 首轮截图显示纸片更像分散卡片，削弱深度推理；已收拢为彼此遮挡的层叠构图并重新执行本关组件与双端浏览器验收。
  - 截图：`artifacts/screenshots/v2-sequential/045-desktop-1440.png`、`045-mobile-390.png`，均在两层已清晰、错误回送刚发生且前层仍模糊时采集并人工检查。
- **仍缺证据**：未知答案玩家能否由透明度、遮挡和切口高度读出“从最深处开始”；物理手机在重叠纸层上拖动焦点片时的命中手感。
- **停止结论**：工程硬门已通过；不增加层号、深浅标签、顺序箭头或步骤提示，进入 046。

### 046 `silent-handoff` — 工程验收通过

- **设计事实**：两只纸手隔着一枚压印小钟相望，光点停在左手向内的外缘；掌心是有意设置的伪按钮，按下任何一只手都会使双手缩回。真正的共享关系是手与手之间宽而浅的纸桥，而不是某个可点击物件。
- **实现结果**：以专属双纸手、四段浅桥、单一跟随光点和掌心退缩反馈替换通用固定路线拖拽。桌面只需让指针连续掠过桥面，手机从左手外缘空白按住并连续拖过，键盘用四次右方向键推动同一枚可见光点；三种输入都按相同的四段因果逐段显色。跳过桥段、途中抬起、分两次拼接路线或按任一掌心都不会累计通关，路径命中宽度为 48px，不以长距离像素精度制造难度。
- **信息层级**：未破解前不显示“不点击的交接”，移除随机环境图标。纸手朝向、左缘光点、压印小钟上方的四段浅痕和随移动显色承担线索；无路线箭头、序号、进度文案或显式“不要点击”提示。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：175 / 175 通过，其中 6 项覆盖双手与 48px 桥结构、双掌点击失败、跳段失败、分段手势不累积、连续按住轨迹和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实掌心点击退缩、两段断开路线失败、桌面真实 hover、手机真实按住拖影，以及视口边界与核心区不遮挡。
  - 首轮浏览器验收发现父场景继承了 `pointer-events:none`，导致桥面轨迹完全收不到事件；已显式恢复桥面命中并重新执行双端验收，避免把组件模拟事件误当作真实可玩证据。
  - 截图：`artifacts/screenshots/v2-sequential/046-desktop-1440.png`、`046-mobile-390.png`，均在两段纸桥已经显色、光点尚未交到右手时采集并人工检查。
- **仍缺证据**：未知答案玩家能否由掌心缩回与浅桥显色推断出“移动而不按下”；物理手机从纸手外缘空白处起笔时是否会出现系统滚动或误触手势。
- **停止结论**：工程硬门已通过；不增加常驻箭头、桥段编号、掌心禁用符号或显式操作文案，进入 047。

### 047 `deep-pressure` — 工程验收通过

- **设计事实**：纸石悬在三层水纹上方，直接按石头只会把石头抬高并扩大涟漪；停止输入后，水纹从运动到收束，原本柔软的投影才逐渐成为可承压纸面。等待只显露作用对象，最终动作必须落在影子上，因此不复用 002 的“等候后按中心”答案。
- **实现结果**：以专属纸石、水面、三层涟漪和独立影子替换通用长按圆点。按石头、过早按影子或滚动会重新扰动水面，普通指针移动不影响 1.3 秒自然沉静；影子稳定后按住 0.9 秒，石头才沉入。提前松手明确回到稳定影子而不误过，随后一轮完整长按即可完成；Space 在影子焦点上执行同一因果。
- **信息层级**：未破解前不显示“等涟漪安静”，移除随机环境图标。石头的上浮、涟漪扩张与收束、影子由浅到深及受压光晕承担线索；无等待倒计时、长按进度数字、目标箭头或答案文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：181 / 181 通过，其中 6 项覆盖专属石/纹/影结构、按石头反效果、移动不扰动、过早按影失败、提前松手与完整影子长按、键盘 Space 等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实错误按压、静置期间普通移动、稳定影子、提前松手、再次完整长按，以及视口边界与核心区不遮挡。
  - 浏览器首轮验收发现 `onDiscover` 回调身份变化会重启沉静 effect，使画面显示“稳定”而内部 ready 状态被清空；已改为稳定 ref 转发回调并重跑组件与双端浏览器验收。
  - 截图：`artifacts/screenshots/v2-sequential/047-desktop-1440.png`、`047-mobile-390.png`，均在影子稳定且长按产生压痕、石头尚未沉入时采集并人工检查。
- **仍缺证据**：未知答案玩家能否区分“等待是显露影子”与“等待本身就是答案”；物理手机长按影子时浏览器手势取消率。
- **停止结论**：工程硬门已通过；不增加等待秒数、影子按钮标签、向下箭头或“按影子”步骤提示，进入 048。

### 048 `pause-word` — 工程验收通过

- **设计事实**：五条带有 `P/A/U/S/E` 局部字形的纸带持续从不同高度掠过，中央只有一块没有文字的空纸保持固定。文字纸带是显眼但错误的作用对象；按它们只会加速，真正的控制面是静止空白。
- **实现结果**：以专属五纸带、错位字形、固定空纸和会合状态替换通用长按圆点。逐一按遍五条纸带只累计加速，不会形成答案；按住空白时纸边产生压痕，0.6 秒后系统自动选择下一会合点并将五条纸带排成 `PAUSE`，无需玩家卡准松手帧。提前松手保留碎片状态，Space 在空白焦点上执行同一路线；减少动态模式把往返改为三档离散位置。
- **信息层级**：未破解前不显示“按住空白”，移除随机环境图标。错位纸带、局部字形、唯一静止的无字纸块和受压凹痕承担线索；无“PAUSE”常驻答案、长按进度、停帧倒计时或目标箭头。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：187 / 187 通过，其中 6 项覆盖五纸带与固定空白、按文字加速、机械遍历失败、提前松手、自动会合和键盘 Space 等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实五纸带错误路径、空白提前松手、再次连续按住自动对齐，以及视口边界与核心区不遮挡。
  - 首轮桌面验收发现动画纸带在特定相位会抢占固定空白的第二次命中；已将空白提升为独立稳定交互层，并用真实 `elementFromPoint` 断言确认桌面与手机中心命中始终归属空白。
  - 截图：`artifacts/screenshots/v2-sequential/048-desktop-1440.png`、`048-mobile-390.png`，均在空白被按出凹痕、字带仍处于碎片状态时采集并人工检查。
- **仍缺证据**：未知答案玩家能否由“按文字变快、空白不动”推断空白是控制面；减少动态偏好下三档位置是否仍足以表达纸带正在掠过。
- **停止结论**：工程硬门已通过；不增加空白描边高亮、暂停图标、文字说明或精确松手要求，进入 049。

### 049 `ten-thousand-glyph` — 工程验收通过

- **设计事实**：大片浅色天空纸上只有五枚小刻点，点间纸纤维构成一条尚未闭合的自然曲线；它不是五个按钮，也不要求临摹精确外形。正式答案是一次连续输入按关系经过五点并回到起点，页面描线始终是完整路线，摄像头只保留为非必要可选输入。
- **实现结果**：以专属五刻点纸场、隐约纤维、逐点墨晕、断口和连续墨线替换通用任意划线。轨迹采用 16% 宽容命中，只验证固定顺序、五点到达、闭合和不中断；错误起点、跳点、走完但不闭合或分两笔拼接均失败。键盘以五次右方向键沿同一可见轨道闭合；没有申请摄像头权限，也没有把摄像头设为唯一解。
- **信息层级**：未破解前不显示“万分之一的线”，移除随机环境图标。五枚小刻点、极浅断开纤维、经过后保留的薄荷墨晕和最后未闭合断口承担线索；无点位编号、箭头、轨迹模板文字、摄像头弹窗或像素评分。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：193 / 193 通过，其中 6 项覆盖五点结构、错误起点、分笔不累积、未闭合失败、宽松连续闭合和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实错误起点、断开的两笔路线、五点未闭合状态、最终连续闭合，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/049-desktop-1440.png`、`049-mobile-390.png`，均在五点已依次着墨、最后闭合段仍为空白断口时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从远观轮廓推断起点与顺序；物理手机一次连续曲线的自然手感及页面滚动抑制效果。
- **停止结论**：工程硬门已通过；不增加点号、方向箭头、描摹轮廓、强制摄像头或像素精度评分，进入 050。

### 050 `mirrored-input` — 工程验收通过

- **设计事实**：中英文两条透明句带从相反方向伸入同一张纸，问号与句号分别位于朝向中央的句末；初始只有一侧句末关系成立，中央共享槽仍空着。拖任一条句带必须让另一条等量反向移动，因此单独满足一种语言、点击标点或切换界面语言都不是答案。
- **实现结果**：以专属双语句带、相反书写方向、两枚句末标点和中央共享槽替换通用三纸层拖动。英文带横向移动 50px 对应一个宽松镜像单位，中文带按相反方向映射；任一真实拖动都会让另一带等量反向移动。只有镜像偏移回到中央宽容区时，两种句末同时正确并激活；单击、仅满足英文或仅满足中文均不会误过。键盘在当前句带上以左右方向键移动同一组可见句带，不提供口令或文本输入框。
- **信息层级**：未破解前不显示“两种标点的同一个位置”，移除随机环境图标。透明材质、相向的句带、书写方向小箭头、错位标点和中央纸槽承担线索；无语言切换机关、答案输入框、目标文案、步骤数字或进度条。桌面位于秒表右侧，手机重排到主按钮下方，核心计时区保持主视觉。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：199 / 199 通过，其中 6 项覆盖双句带结构、点击无效、只满足单语失败、等量反向移动、共享槽成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实单击无效、真实拖动只满足英文仍不通关、继续反向拖回共享槽后双语同时成立，以及视口边界与核心区不遮挡。
  - 首轮浏览器验收在两条带交叠后从元素外接框中心继续抓取，实际命中上层中文带；测试改为从仍可见且未被覆盖的英文纸边续拖，并保留 `elementFromPoint` 真实命中断言。机制未放宽，双端复测通过。
  - 截图：`artifacts/screenshots/v2-sequential/050-desktop-1440.png`、`050-mobile-390.png`，均在英文句末正确、中文仍错位、共享槽尚未填满时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从相向箭头与同步反向位移推断“两个句末共用中央位置”；物理手机在两条透明带重叠后从可见纸边续拖的手感。
- **停止结论**：工程硬门已通过；不增加“双语同时成立”文案、目标高亮、语言切换按钮或拖动步骤，进入 051。

### 051 `ready-code` — 工程验收通过

- **设计事实**：`READY` 的首枚 `R` 字牌并非普通字母按钮，而是一枚宽度约为其他字牌两倍、中央有装订纸缝的合拢双页。点击字母、键入口令或拖走整枚 `R` 都不能改变词义；必须分别沿纸缝向左右展开，才会从厚字牌内页依次露出 `S`、`T`，与保持原位的 `EADY` 组成 `STEADY`。
- **实现结果**：以专属五块活版字、加厚双页、中央虚线装订、左右内页和轻微纸面抖动替换通用折叠按钮。左右半页各有独立宽松抓取面：外拉 40px 吸附展开，向内拖超过 20px 沿折痕回弹；轻点和小于 12px 的抖动不进入发现。只开左页显示 `S·EADY` 且继续抖动，只开右页同样不通关；两页都展开才稳定为 `STEADY`。键盘用左页 ArrowLeft、右页 ArrowRight 表达同一空间关系。中文版正面独立显示“就绪”、内页显示“稳定”，不依赖英文字形变形。
- **信息层级**：未破解前不显示“准备好以后再稳一点”，移除随机环境图标。首牌双倍宽度、中央装订点、双层纸厚、半开时露出的内页和抖动停止承担线索；无输入框、答案按钮、展开箭头、步骤文案或字母点击计数。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：205 / 205 通过，其中 6 项覆盖加厚双页结构、点击无效、向内回弹、单页不完成、双页组成 STEADY 和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击、向内错误拖动、只展开左页和最终双页展开，以及视口边界与核心区不遮挡。
  - 首轮手机浏览器验收测得单侧折页抓取宽度 43.22px，未放宽 44px 触控硬门；已增大移动端加厚字牌并复跑双端，左右折页均满足最小命中尺寸。
  - 截图：`artifacts/screenshots/v2-sequential/051-desktop-1440.png`、`051-mobile-390.png`，均在左内页 `S` 已显、右页仍保持 `R` 正面、整词尚未稳定时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从首牌宽度与装订纸缝推断它能向两侧打开；物理触屏在已打开一侧后抓取另一半页的手感。
- **停止结论**：工程硬门已通过；不增加“展开 R”提示、左右箭头、文本输入或逐字高亮，进入 052。

### 052 `bend-command` — 工程验收通过

- **设计事实**：`RUSH` 写在一条连续长纸带上，只有首字母所在纸面横穿一道带装订点的折线；纸背预先独立印着 `H`。玩家不需要拼写输入或逐字点击，只需把折线向上弯起，让背面字形盖住正面 `R`，其余 `USH` 原位不动。中文版以正面“急”和背面“静”的独立字图表达同一纸背改写因果。
- **实现结果**：以专属命令纸带、首格正背两面、可抓折线和单次向上吸附替换通用折叠按钮。点击不进入发现；横向拖动会沿折痕回弹并清空折叠量。向上移动时折叠进度按 60px 宽松距离连续变化，30px 半折已经露出独立背面但不通关，达到 48px 且横向偏差不超过 65px 才吸附完成 `HUSH`。键盘聚焦可见折线后按 Enter 完成同一个折纸动作。
- **信息层级**：未破解前不显示“把急字折慢”，移除随机环境图标。长纸带连续边缘、只有首格存在的折线、正背材质差和半折时同时可见的 `R/H` 承担线索；无输入框、目标单词、方向箭头、步骤数字或拼写提示。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：211 / 211 通过，其中 6 项覆盖单纸带结构、点击无效、横拉回弹、半折背面、宽松完成和 Enter 等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击、横向错误拖动、30px 半折不完成和 60px 向上折叠成功，以及视口边界与核心区不遮挡。
  - 首轮截图中半折只显出薄荷纸背，`H` 可读部分不足；已增强仅在过渡态的背面字形露出，不添加说明文字，并重新执行双端验收与截图目检。
  - 截图：`artifacts/screenshots/v2-sequential/052-desktop-1440.png`、`052-mobile-390.png`，均在 `R` 正面仍保留、背面 `H` 已从折线下方显出但尚未吸附时采集并人工检查。
- **仍缺证据**：未知答案玩家是否会把首格折线与背面 `H/静` 联系成“纸背改写”；物理手机向上折纸时是否会与页面滚动产生竞争。
- **停止结论**：工程硬门已通过；不增加向上箭头、拼写输入、逐字按钮或折叠百分比，进入 053。

### 053 `override-command` — 工程验收通过

- **设计事实**：外层 `SLOW` 纸环与内层“慢”纸环绕同一个空心位置高速反向转动，旁边只有一枚松开的纸轴钉。拖动任一纸环只会把速度转给另一环，切换语言、点击文字或分别干预两环都不能停止；只有把轴钉装入两层共享的空心，反向运动才会在同一轴上抵消。
- **实现结果**：以专属双语同心纸环、相反方向纹理、共享空心、啮合纸齿和可自由二维拖动的轴钉替换通用横向拖拽。纸环点击完全无效；真实拖动超过 12px 只交换两环快慢并保持 `opposed`。轴钉落到纸环边会歪斜并回到原处，进入中心 12% 宽容区才锁定并同时停下两环。键盘方向键移动同一枚可见轴钉，两次左、一次上后 Enter 可进入相同中心宽容区。
- **信息层级**：未破解前不显示“快慢同意”，移除随机环境图标。双语文字、相反旋向符号、两种纸纹、共同空心和靠近时显出的啮合纹承担线索；无目标箭头、步骤、速度数字、语言切换答案或显式“放中心”文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：217 / 217 通过，其中 6 项覆盖双环与共享轴结构、点击无效、拖环仅转速、错误落点回弹、宽松中心锁定和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实纸环点击、真实拖环、轴钉落到纸环歪斜退回、再装入共享中心，以及视口边界与核心区不遮挡。
  - 首轮手机截图暴露旋转关键帧没有显式起点，动画插值会使纸环临时偏离中心并被纸场裁切；已把静态与动画起点都固定在共同中心，重新执行双端验收和截图目检。
  - 截图：`artifacts/screenshots/v2-sequential/053-desktop-1440.png`、`053-mobile-390.png`，均在轴钉被拖到纸环边、呈歪斜反馈但尚未装入中心时采集并人工检查。
- **仍缺证据**：未知答案玩家能否由“拖一环只把速度交给另一环”和共享空心推断应装轴；物理手机把小轴钉拖入同心环时的抓取手感。
- **停止结论**：工程硬门已通过；不增加中心目标圈高亮、速度读数、操作箭头或答案文案，进入 054。

### 054 `hybrid-console` — 工程验收通过

- **设计事实**：中央纸轴会把绕上的丝带弹回，纸轴中心有按压纹，丝带末端有环行纸印。单独按轴只让轴安静，单独绕丝带只留下会退回的压痕；固定纸轴与绕行丝带必须在同一段时间内持续成立，不能先后拼接两个半圈。
- **实现结果**：以专属按压纸轴、宽松四区环道、连续丝带压痕和独立丝带端替换通用单物件拖拽。未固定纸轴时完整绕行只回弹；释放纸轴会立即清除尚未完成的绕线，不能跨两次输入累计。按住纸轴后，丝带按右→下→左→上→右的自然环序经过四个宽区即可完成，不比较精细角度或速度。桌面支持 Space 固定轴并用指针绕行，也支持指针按轴加方向键；多指触摸走同一双角色事件模型。减少动态偏好下连续按轴 5 秒会锁定轴，再允许单指绕行。
- **信息层级**：未破解前不显示“一只手按住，另一只手绕行”，移除随机环境图标。轴心按压纹、丝带末端环印、四处浅纸扣、按下凹陷和回弹压痕承担线索；无手势步骤图、进度数字、倒计时、输入口令或常驻方向箭头。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：224 / 224 通过，其中 7 项覆盖专属双角色结构、未按轴回弹、只按轴无效、释放轴清除半圈、重叠输入成功、方向键等价路线和减少动态 5 秒锁轴。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实未固定绕行回弹、Space 与真实指针同时成立、半圈状态和完整连续绕行，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/054-desktop-1440.png`、`054-mobile-390.png`，均在纸轴持续按下、丝带已绕半圈但尚未完成时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把轴的回弹与丝带退回联系成“需要同时固定”；物理手机双指固定加绕行的手感及减少动态锁轴是否容易理解。
- **停止结论**：工程硬门已通过；不增加双手图标、环行教程、圈数数字或显式“同时操作”文案，进入 055。

### 055 `nineteen-code` — 工程验收通过

- **设计事实**：半透明 `1` 与 `9` 分居纸面两侧，内侧切口分别保留直线与圆的形状关系。正面分开时清楚可读，却只投出混在一起的影；把两片正面靠近并重叠后，正面越来越难辨，影子反而分离并清楚读成 `10`。答案不是输入或计算十九。
- **实现结果**：以专属双字形透明纸片、内侧虚切线、固定投影纸面和反向清晰度关系替换通用三纸层。点击两片、键入 `19/10` 或向外拖动都不能完成；向内移动进入 28% 范围时影子开始分离，中心距不超过 12% 的宽松重叠区才显示清晰 `10` 并激活。指针可以拖任一字片，键盘在同一可见字片上用向内方向键靠拢，不弹数字输入框。
- **信息层级**：未破解前不显示“十九之间”，移除随机环境图标。巨大半透明字形、内侧切口、正面重叠后的混色和下方由糊到清的影子承担线索；无算式、输入框、目标区域、移动箭头或解释文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：230 / 230 通过，其中 6 项覆盖双字形/双影结构、点击与数字键无效、向外更清、部分重叠、宽松完整重叠和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击和键入无效、向外拖动、部分重叠的影子显形、继续重叠成功，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/055-desktop-1440.png`、`055-mobile-390.png`，均在两片字形已有轻微交叠、影子刚能模糊读成 `10` 但尚未完成时采集并人工检查。
- **仍缺证据**：未知答案玩家能否接受“正面更乱而影子更清”的反直觉关系并继续重叠；物理手机抓住半透明重叠纸片时的层级感。
- **停止结论**：工程硬门已通过；不增加“看影子”文字、算式、显式重叠目标或拖动箭头，进入 056。

### 056 `hundred-code` — 工程验收通过

- **设计事实**：纸条正面固定印着 `1000`，纸条下方一枚微小墨点初看只是装饰。墨点不是点击按钮，也不接受数字输入；把它移动到四个数字间隙之一，才会成为纸条的一部分并改变读法。只有第一与第二个 `0` 之间能稳定读成 `10.00`，其余三个间隙分别保留 `1.000`、`100.0`、`1000.` 的错误预览。
- **实现结果**：以专属长纸条、四枚等宽印刷数字、四个真实字间落点和可自由移动墨点替换通用图标路线。点击墨点、键入数字或把墨点放到错误间隙都不会完成；错误落点保留对应排版，允许玩家比较标点位置而不清空探索。指针可直接拖放，键盘用左右键移动同一枚可见墨点后按 Enter 落下，不弹输入框。
- **信息层级**：未破解前不显示“把点放进 1000”或目标数值说明，移除随机环境图标。纸条边缘、四个自然字间、墨点与错误排版预览承担线索；无目标框、方向箭头、间隙编号、答案按钮或输入控件。桌面位于秒表右侧，手机位于主按钮下方，不覆盖核心计时区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：236 / 236 通过，其中 6 项覆盖专属纸条结构、点击与键入无效、两种错误小数预览、正确 `10.00` 和键盘等价路径。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实错误落点会保留不同预览、正确间隙完成，以及视口边界与核心区不遮挡。
  - 首轮截图发现墨点与数字字形存在视觉叠压，错误预览看起来像“点在数字上”而不是“点在间隙”；已把数字改为四个等宽槽并重新校准四个间隙坐标，随后重跑组件、双端浏览器验收和目检。
  - 截图：`artifacts/screenshots/v2-sequential/056-desktop-1440.png`、`056-mobile-390.png`，均在墨点落入错误间隙、纸条保留 `100.0` 预览时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从“纸外墨点”和错误排版比较自然推断出小数点；物理手机拖放小墨点时的命中手感。
- **停止结论**：工程硬门已通过；不增加小数点箭头、间隙编号、答案示例或数字输入，进入 057。

### 057 `five-bit-latch` — 工程验收通过

- **设计事实**：五枚带黑白与凸凹双重编码的位元片排在同一纸槽，正面序列固定为不对称的 `10110`；五道投影共用上方唯一光源。逐片翻动只是症状级操作，真正关系是移动共享光源：靠近纸槽中心时外侧位元先成对压入，居中后投影与正面组合成 `10101` 回文锁形，纸槽闭合。
- **实现结果**：以专属五位纸槽、五枚独立纹理位元、五道同步影子、单一可拖光源和成对压入反馈替换通用单纸片拖拽。逐一点击五片只触发回弹且不累计；光源偏置时五道影子整体同向改变，进入宽松近中心带先压入外侧一对，进入中心带才压入两对并闭锁。指针拖动同一光源，键盘左右键移动同一可见物件，不提供五个开关输入。
- **信息层级**：未破解前不显示“让影子锁住位元”或“公平位置”，移除随机环境图标。光源轨缝、倾斜光束、位元凸凹纹理、整体位移的影子及成对下沉承担线索；无目标中心高亮、二进制答案、方向箭头、进度数字或步骤文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：242 / 242 通过，其中 6 项覆盖五片/五影/单光结构、逐片点击回弹、共享影子偏移、近中心单对压入、中心闭锁和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证五片真实点击不累计、光源远移仍不完成、近中心半反馈、居中完成、44px 光源命中，以及视口边界与核心区不遮挡。
  - 首轮浏览器验收发现光源的 80ms 位置过渡会让紧接着的第二次拖动从旧视觉坐标起笔；验收按真实视觉稳定边界等待 120ms 后继续，并在双端复测通过。该边界低于用户推理与再次抓取所需时间，不增加操作延迟或隐藏状态。
  - 截图：`artifacts/screenshots/v2-sequential/057-desktop-1440.png`、`057-mobile-390.png`，均在光源接近中心、外侧一对位元已经压入但纸槽尚未闭合时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把“逐片回弹”和“五影同步移动”联结成共享光源推理；物理手机上观察凸凹纹理与连续拖光的手感。
- **停止结论**：工程硬门已通过；不增加中心靶标、回文数字答案、逐位开关或显式“移动光源”文案，进入 058。

### 058 `cipher-reversal` — 工程验收通过

- **设计事实**：一整条透明词带正面按 `WOLS` 排列，直接阅读没有意义；纸后压扁的投影从右向左却能读成 `SLOW`，右上角只有一处真实折痕。单字不是独立答案，拖动任何单字只会让背面投影更模糊；沿折角一次翻转整条纸带，背面才成为正面。中文版用四枚可整体反转的图形短句表达相同关系，不要求玩家镜像阅读汉字。
- **实现结果**：以专属透明双面纸带、正面/背面两套字形、背后反向投影、四块透明命中层与单一折角替换通用折叠按钮。逐字点击和键入 `slow` 均无效；拖动单字只让影字偏移变差。折角按纸带宽度连续控制整体翻转，半翻同时保留正反方向，未过宽松阈值会回弹，跨过阈值一次完成；Enter 操作同一可见折角，不弹输入框。
- **信息层级**：未破解前不显示“把整句话翻过来”或目标单词，移除随机环境图标。透明材质、纸后压扁影字、右上折痕和半翻时的双面关系承担线索；无逐字排序槽、输入框、翻转箭头、答案按钮或步骤文案。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：248 / 248 通过，其中 6 项覆盖单纸带/双读向/折角结构、逐字与输入无效、单字扰乱投影、半翻回弹、整体翻面和 Enter 等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实四字点击与键入无效、单字拖动让影字变差、半翻双读向、回弹、完整翻面，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/058-desktop-1440.png`、`058-mobile-390.png`，均在单字投影已被扰乱、整条纸带正在半翻且尚未完成时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从“单字越动越糟”和纸角折痕推断整体翻面；物理手机沿小折角轻扫时是否自然且不会与页面滚动冲突。
- **停止结论**：工程硬门已通过；不增加翻面箭头、目标单词、逐字排序槽或输入控件，进入 059。

### 059 `chronos-command` — 工程验收通过

- **设计事实**：十格钟面上有三片透明弧，长度固定跨越 2、3、5 格，中心只写目标 `10`；弧片初始散落并同时产生重叠与空缺。玩家应移动整段弧长，让三片在同一圆周首尾相接、每一格恰好覆盖一次；起点不固定，任何整体旋转等价解都成立。
- **实现结果**：以专属十点钟面、同一圆周上的三段半透明弧、三个随起点移动的宽松抓手和逐格覆盖点替换通用轨道拖拽。点击标有 2/3/5 的抓手不计旧口令；拖动一片会按落点吸附到最近刻度，重叠处计数点合并变色，接上一段时进入形成状态。覆盖数组严格验证十格各一次，因此 `[0,2,5]` 与整体旋转后的 `[1,3,6]` 均通过；键盘左右键逐格旋转同一可见弧片。
- **信息层级**：未破解前不显示“钟面凑十”或 2+3+5 算式，移除随机环境图标。弧片实际长度、中心 `10`、空点、合并点和首尾相接的同圆轨道承担线索；无起点编号、目标槽、顺序箭头、算式或步骤文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：254 / 254 通过，其中 6 项覆盖十格/三弧结构、旧 2-3-5 点击无效、重叠点合并、标准完整环、旋转等价解和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击无效、三格弧制造重叠、首个接合状态、五格弧补满十格、44px 抓手，以及视口边界与核心区不遮挡。
  - 首轮截图发现三段弧被画在明显不同半径上，视觉上像三个同心圈而不是同一钟面的首尾拼接；已将三段统一到同一圆周并用半透明混色表达重叠，随后重跑双端验收与截图目检。
  - 截图：`artifacts/screenshots/v2-sequential/059-desktop-1440.png`、`059-mobile-390.png`，均在 2 格与 3 格弧已经接合、5 格弧仍造成重叠与空缺时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从中心 `10`、弧长和计数点自然推断“覆盖十格而非点击数字”；物理手机沿圆周拖动抓手的手感。
- **停止结论**：工程硬门已通过；不增加算式、刻度编号、固定起点、顺序箭头或答案提示，进入 060。

### 060 `cipher-knot` — 工程验收通过

- **设计事实**：同一条印字纸带在中央只打了一个上下层次错误的结，五枚字形因此沿回环被拆散；两端是同一纸带的自由端，不是两根独立绳索。拖动任一自由端会改变结的张力和字距，但只有一次真正穿过中央交叉并交换上下层关系，纸带才会展开成 `PAUSE`。
- **实现结果**：以一条连续自交 SVG 主路径、仅覆盖中央短段的上层纸带、两枚宽松自由端和分布在回环上的 `P/A/U/S/E` 替换通用拖轨。点击交叉点、点击端点、键入 `pause` 均无效；只拉一端会连续改变张力与字距但不会完成。指针路线必须横跨中央并产生明确纵向换层，才交换唯一错误交叉并展开；焦点落在任一自由端后按 Enter/Space 执行同一语义动作。
- **信息层级**：未破解前不显示“交换上下层”或目标单词，移除随机环境图标。连续纸带、中央压印、短上层覆盖、两端张力与碎片化阅读承担线索；无交叉箭头、逐字排序槽、输入框、答案按钮或步骤文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：260 / 260 通过，其中 6 项覆盖单纸带/单交叉/双自由端结构、点击与输入无效、仅拉紧不完成、错误穿越、正确换层和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击与输入无效、仅改变张力、错误穿越仍保留结、正确中心换层完成、44px 自由端，以及视口边界与核心区不遮挡。
  - 首轮截图发现上下两段绘制得过于完整，视觉上像两条平行纸带而不是同一条自交纸带；已改成一条连续主路径加中央短覆盖段，随后重跑双端验收并重新目检。
  - 截图：`artifacts/screenshots/v2-sequential/060-desktop-1440.png`、`060-mobile-390.png`，均在纸带已被拉紧、唯一交叉仍未换层时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从连续纸带和唯一压印自然推断“交换中央上下层”；物理手机以单指跨过结心时的纸带跟手感。
- **停止结论**：工程硬门已通过；不增加交叉箭头、目标单词、逐字排序或输入控件，进入 061。

### 061 `reverse-sweep` — 工程验收通过

- **设计事实**：一把从 `9` 到 `0` 反写方向的纸尺只有一处缺失刻度，尺上方云影也只有一个透光缺口；扫描线与云影采用反向控制，手向右时缺口向左。通关条件不是方向口令或累计拖动距离，而是让可见云影缺口与唯一空刻度真实重合。
- **实现结果**：以专属反向纸尺、十枚真实刻度（其中一枚缺失）、连续云影、透光缺口和一根宽松扫描线替换通用单物件拖轨。点击扫描线、键入 `slow` 和来回刷距离均无效；扫描线与缺口始终按 `x` 与 `100-x` 同步反向，错误方向不会重置且可连续修正。距离空刻度 12% 内邻近刻度开始升温，4% 宽松范围内才完成；滚轮和左右键移动同一根可见扫描线。
- **信息层级**：未破解前不显示“你的手和影子走向相反”或关卡名，移除随机环境图标。反写数字、唯一断刻、扫描线与缺口的即时反向位移、邻近刻度升温承担线索；无方向箭头、目标框、距离进度、口令或步骤文案。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：267 / 267 通过，其中 7 项覆盖反写纸尺/唯一断刻/云影缺口结构、点击输入与累计距离无效、反向映射、错误方向可修正、几何重合、滚轮和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击与输入无效、先向错误方向移动、再反向接近并最终对齐、44px 扫描线，以及视口边界与核心区不遮挡。
  - 首轮截图在扫描线 80ms 位置过渡尚未稳定时采集，视觉位置落在旧坐标与新坐标之间；已把截图时点移到过渡完成后并重跑双端验收，保证证据与 `data-scan-position=65` 一致。
  - 截图：`artifacts/screenshots/v2-sequential/061-desktop-1440.png`、`061-mobile-390.png`，均在云影缺口已接近空刻度、邻近刻度开始升温但尚未完成时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把第一次拖动的反向位移与反写刻度联系起来，并在没有箭头时注意唯一断刻；物理手机水平拖动与页面纵向滚动的手感。
- **停止结论**：工程硬门已通过；不增加方向箭头、断刻高亮、距离进度或显式反向说明，进入 062。

### 062 `pointer-echo` — 工程验收通过

- **设计事实**：一个实心纸指针后方始终跟着约 600ms 延迟的半透明纸影，画面另一侧只有一枚浅窝。浅窝拒绝实心指针，即使纸影已经追到它身下也不能完成；必须让实心指针离开而延迟纸影留在浅窝，目标才显色并激活。
- **实现结果**：以专属实心标记、独立延迟位置队列、半透明纸影、三段弱轨迹和浅窝替换通用同步拖块。鼠标无需按下即可带动实心标记，触摸拖动使用同一二维场地；每个真实位置在 600ms 后按顺序传给纸影。实心落槽只产生凹压拒绝，先离开再等纸影或等纸影到达后再离开均可完成；点击和键入 `echo` 无效。键盘方向键移动同一实心标记，减少动态模式把延迟表达为三段位置而非连续追逐动画。
- **信息层级**：未破解前不显示“后面还有一个你”或关卡名，移除随机环境图标。实心/半透明材质差异、移动后继续追来的纸影、只对纸影显色的浅窝和三枚渐弱轨迹承担线索；无等待倒计时、目标箭头、拖动教程、进度数字或步骤弹窗。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：274 / 274 通过，其中 7 项覆盖双标记/浅窝/三段轨迹结构、点击输入无效、600ms 延迟、实心拒绝、先离开后落影、落影后离开和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标跟随与延迟追上、触点拖入浅窝被拒绝、移出后纸影落槽、二维操作面尺寸，以及视口边界与核心区不遮挡。
  - 首轮浏览器断言把“截图后仍未完成”绑定在 600ms 窗口内，但截图本身可能跨过该窗口，造成双端同样的时序失败；验收已改在实心仍停在浅窝时采集稳定的拒绝状态，再移出并等待纸影完成，规则与生产实现未放宽。
  - 截图：`artifacts/screenshots/v2-sequential/062-desktop-1440.png`、`062-mobile-390.png`，均在实心标记停于浅窝、延迟纸影尚在后方追赶且目标明确拒绝实心时采集并人工检查。
- **仍缺证据**：未知答案玩家能否在没有倒计时的情况下注意到约 600ms 延迟，并推断目标只接受纸影；物理手机单指拖动、松手与页面滚动之间的手感。
- **停止结论**：工程硬门已通过；不增加等待倒计时、纸影文字标签、目标箭头或显式“本人离开”说明，进入 063。

### 063 `corner-zigzag` — 工程验收通过

- **设计事实**：一张独立纸页的四个角各有一条同斜率切口，中央折线缝却断成四段；角不是四个点击按钮。玩家把唯一活动线端按切口关系逐边送出纸页，线端会在相邻角继续出现，已经接好的段保留，最终四段闭合。
- **实现结果**：以专属四角纸页、四枚平行斜切口、四段独立虚线缝和一枚 48px 活动线端替换通用自由描线。点击线端不推进；每次只需从当前角短拖到相邻正确边缘，错误边缘触发原路回弹且不抹去此前已接段。四段顺序由可见切口与当前角共同约束，全部保留后闭合；键盘方向键选择同一相邻边缘，不要求一次长描或像素级轨迹。
- **信息层级**：未破解前不显示“角不是终点”或关卡名，移除随机环境图标。四条平行切口、当前活动线端、已接实线与未接虚线承担线索；无边缘编号、顺序箭头、步骤计数、目标高亮或整条轨道教程。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：280 / 280 通过，其中 6 项覆盖四切口/四断段结构、点击与错误边缘无效、正确边缘保留、后续错误不清零、四边闭合和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实错误边缘回弹、首段换角、后续错误保留、逐边闭合、48px 活动端，以及视口边界与核心区不遮挡。
  - 首轮双端验收中桌面通过，手机最后一拖在左下角同时进入“左边缘”和“下边缘”宽容区，被旧固定优先级误判为下边缘；已改为角点双重命中时优先当前可见切口指向的边缘，随后双端复测通过。视觉复核还发现四角切口原先交替斜率，已统一为同向平行切口并再次重跑双端截图。
  - 截图：`artifacts/screenshots/v2-sequential/063-desktop-1440.png`、`063-mobile-390.png`，均在上边与右边两段已接实线、下边与左边仍为断续虚线时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从四条平行切口推断角点是跨边转弯而非装饰；物理手机在纸页边缘短拖时是否会误触页面滚动或系统边缘手势。
- **停止结论**：工程硬门已通过；不增加边缘编号、方向箭头、步骤计数或完整轨道高亮，进入 064。

### 064 `hinge-loop` — 工程验收通过

- **设计事实**：两张纸板共用一根真实铰链，正面两枚半环朝向相同，因此原位平移永远无法闭合。只有把右板绕共同铰链翻到背面，背面的反向半环才会跨过铰链与左板拼成完整圆环。
- **实现结果**：以专属双板、中央铰链轴、正反两面和三态圆环替换通用折叠按钮。点击左右纸板与向外平移都无效；向铰链方向短翻会即时看到背面和圆环跨轴生长，但松手回弹。翻过宽松吸附阈值后右板自动落到 180° 背面并闭环；聚焦右板按 Enter/Space 执行同一铰链翻转语义，不依赖精确三维手势。
- **信息层级**：未破解前不显示“铰链的另一面”或关卡名，移除随机环境图标。共同铰链、两块纸板的真实前后层次、同向半环与翻动时逐渐出现的反向半环承担线索；无翻转箭头、角度数字、目标轮廓、步骤卡或显式“翻到背面”说明。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：286 / 286 通过，其中 6 项覆盖双板/共铰链/正反面结构、点击与左板移动无效、向外平移无效、短翻回弹、宽松吸附闭环和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击、错误方向拖动、短翻中间态、松手回弹、长翻吸附、44px 操作面，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/064-desktop-1440.png`、`064-mobile-390.png`，均在右板翻至中段、背面反向半环开始跨铰链生长但尚未吸附闭合时采集并人工检查；双端场景层级清楚且未抢占秒表。
- **仍缺证据**：未知答案玩家能否从“正面同向半环 + 中央真实铰链”自然推断需要翻面而非继续平移；物理手机横向向内翻动时与系统返回手势的冲突感。
- **停止结论**：工程硬门已通过；不增加翻转箭头、角度进度、背面标签或目标环高亮，进入 065。

### 065 `long-archive-route` — 工程验收通过

- **设计事实**：五枚无编号档案标签的文字会按节拍变化，文字本身不可记忆；聚焦一枚标签时，投向下一枚的三角纸影保持稳定。五枚标签构成每次布局都会变化的单向链，只有一个无入边起点和一个无出边终点，玩家要读影向而不是读标签。
- **实现结果**：以专属五标签折页地图、三组三态变词、四枚稳定三角影、保留式纸纹和错误折返影替换固定三个焦点按钮。布局从六条非恒等路线中会话内严格轮换，重新进入不会复用刚才的序列；点击打开标签只累计无效打开次数，不能推进。错误起点或分支会清除已保留纸纹并显出折返影；桌面 hover/focus、键盘 Tab 顺序和手机按住连续滑过均沿同一影向链完成。
- **信息层级**：未破解前不显示“读影子，不读标签”或关卡名，移除随机环境图标。持续变动的短词、聚焦时出现的三角影、正确经过后保留的纸纹和错误死端短暂折返承担线索；无标签编号、固定连线、方向说明、路线进度或档案内容弹窗。桌面位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：293 / 293 通过，其中 7 项覆盖五标签/四稳定影向结构、文字变化但影向不变、点击与固定数字遍历无效、错误死端、完整焦点链、手机连续按住滑过及错误后可恢复。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证重新加载路线必变、标签文字变化、点击不推进、错误焦点折返、逐影保留纸纹、44px 标签操作面，以及视口边界与核心区不遮挡。
  - 首轮浏览器验收发现开发模式双重挂载可能让随机选择在重载后回到旧布局，同时指针捕获让普通点击被误判为滑动；已改为会话内严格轮换布局，并以 12px 真实位移区分点击和连续滑动，双端复测通过。
  - 截图：`artifacts/screenshots/v2-sequential/065-desktop-1440.png`、`065-mobile-390.png`，均在前两枚标签已按影向经过、纸纹与两段方向关系已保留但尚未到终点时采集并人工检查。
- **仍缺证据**：未知答案玩家是否会主动逐枚聚焦并把“文字变、影子不变”归纳成路线规则；物理手机按住跨越五张标签时的滚动抑制与手指遮挡程度。
- **停止结论**：工程硬门已通过；不增加标签编号、常驻完整路线、起点高亮、步骤计数或显式“读影子”说明，进入 066。

### 066 `archive-knot` — 工程验收通过

- **设计事实**：四条档案色带的左端和右侧出口已经按点纹、斜纹、格纹和短划纹一一匹配，复杂感来自它们在中央汇成一个错误的上下交叉，而不是端点接错。玩家探查端点只能看见匹配关系和张力回弹；真正答案是只提起中央上层一次，交换唯一错误层级。
- **实现结果**：以专属四条纹理色带、四枚 44px 可探查端点、四枚同纹出口、单一中央上层纸片和张力释放状态替换通用三纸层。点击端点或中央纸片均无效；任一端点横向拉动超过 12px 会拉紧对应带并回弹，四次遍历仍不推进。中央短提显示离层但松手回弹，向上提过宽松 28px 阈值才交换上下关系并让四条带自动舒展；Enter/Space 对聚焦的同一中央纸片执行等价换层。
- **信息层级**：未破解前不显示“端点没有接错”“只动中央”或关卡名，移除随机环境图标。四组明显不同且左右对应的材质、端点回弹、中央唯一短覆层和提起时其他带的张力变化承担线索；无端点编号、连线任务、逐带进度、中央箭头或答案按钮。桌面位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：299 / 299 通过，其中 6 项覆盖四组匹配端点/出口/色带结构、点击与文字输入无效、四端遍历回弹、短提回弹、单次正确换层和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实端点点击与拉动、中央点击、短提、宽松提起换层、44px 操作面，以及视口边界与核心区不遮挡。
  - 首轮截图目检发现左右端点虽有颜色差异，但纹理匹配偏弱；已增强为点纹、斜纹、格纹和短划纹四组对应关系，不增加编号或固定连线，并重跑双端验收与截图。
  - 截图：`artifacts/screenshots/v2-sequential/066-desktop-1440.png`、`066-mobile-390.png`，均在端点探查回弹后、中央上层被短暂提起但尚未换层时采集并人工检查。
- **仍缺证据**：未知答案玩家能否在复杂交叉中先验证端点匹配，再把注意力收束到唯一中央层级；物理手机提起中央短纸片时手指遮挡是否影响观察其他色带释放张力。
- **停止结论**：工程硬门已通过；不增加端点编号、固定连线、中央箭头、逐带连接任务或显式“只动一个交叉”提示，进入 067。

### 067 `wheel-echo` — 工程验收通过

- **设计事实**：纸轮缺失一段半透明弧，实体涟漪与缺口材质不一致；在谜题纸面滚动或纵向滑动时，实体涟漪先运动，半透明回声约 120ms 后向相反方向返回。实体即使经过缺口也会散开，只有返回的半透明回声能够被缺口吸附。
- **实现结果**：以专属纸轮、半透明缺口、实体弧片、延迟回声和局部滚动/触摸控制替换通用滚轮按钮。任意一致方向的四次滚轮、四次短滑或上下方向键都能让实体先到缺口并散开，回声延迟 120ms 后从反向回到同一缺口并吸附；直接点击无效，页面外滚轮不被拦截，也不会推进谜题。
- **信息层级**：未破解前不显示“滚轮回声”或操作说明，移除随机环境图标。纸轮的材质差异、实体先行、回声滞后与反向运动承担线索；无方向箭头、次数、角度数字、进度条或目标高亮。桌面场景位于秒表左侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：306 / 306 通过，其中 7 项覆盖三种材质、局部滚动边界、120ms 反向回声、双方向滚轮、纵向滑动与键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证桌面真实滚轮、手机浏览器原生连续触摸、实体先散开、延迟回声后吸附，以及视口边界与核心区不遮挡。
  - 手机端首次用鼠标指针模拟连续触摸时，第二次指针序列未由 Chromium 手机仿真继续派发；改用浏览器原生 `Input.dispatchTouchEvent` 后，连续四次短滑与真实触摸语义稳定通过。生产组件逻辑无需为测试模拟器增加旁路。
  - 截图：`artifacts/screenshots/v2-sequential/067-desktop-1440.png`、`067-mobile-390.png`，均在实体与回声已经分离、回声刚完成一次反向返回但尚未填补缺口时采集并人工检查。
- **仍缺证据**：未知答案玩家是否能把“材质不同 + 时间错位 + 反向返回”归纳为只有回声能补缺口；不同物理触控板的滚轮增量是否需要节流以获得更清楚的逐步运动感。
- **停止结论**：工程硬门已通过；不增加滚动箭头、方向说明、次数计数、角度读数或缺口高亮，进入 068。

### 068 `counterclockwise-breach` — 工程验收通过

- **设计事实**：钟面宽纸环缺一段，透明弧仍藏在相邻纸背。顺时针擦过缺口会把弧段推得更深，逆时针短扫才把同一实体逐步拉回；成功依据是弧段真实进入缺口，不是累计手势角度或拖动距离。
- **实现结果**：以专属缺口钟面、纸背暗袋、半透明切口和三段式实体弧层替换通用轨道。玩家可在宽环上做三次宽松逆时针短扫完成，顺时针会让已露出的弧退一格但不全盘清零；场景内滚轮向上/向下和左右方向键表达相同的拉回/推远关系。点击、过短移动和远离宽环的累计拖动均无效。
- **信息层级**：未破解前不显示“逆时针拉回缺口”、方向文字或答案，移除随机环境图标。纸环的真实断口、藏在纸背的异材质弧段、每次露出/退回与最终吸附承担线索；无箭头、角度、次数、进度条或目标轮廓。桌面场景位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：312 / 312 通过，其中 6 项覆盖纸背层级、点击/远处累计无效、三次逆时针实体推进、顺时针退格、局部滚轮和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标短扫、浏览器原生触摸短扫、正向露出、反向退回、实体吸附，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/068-desktop-1440.png`、`068-mobile-390.png`，均在一次逆时针拉回后、纸背弧露出第一段但尚未封口时采集并人工检查；断口、背面暗袋和弧段层级可读且未抢占秒表。
- **仍缺证据**：未知答案玩家能否由顺/逆两个方向的露出差异自然推理“拉回掉队的部分”；真实手机单指沿上半环横扫时，手指遮挡是否影响观察弧段的逐步露出。
- **停止结论**：工程硬门已通过；不增加逆时针箭头、次数提示、手势轨迹、角度累计或缺口高亮，进入 069。

### 069 `archive-figure-eight` — 工程验收通过

- **设计事实**：左右两枚泪滴形档案章各缺一段，但共用中央交叉压痕。玩家要用同一笔覆盖两瓣、从相反关系两次经过共同中心并回到起点附近；判定拓扑，不要求临摹标准数字 8。
- **实现结果**：以专属双瓣档案印、共同中心压印、断墨轨道、连续墨迹与抬笔保留层替换通用曲线路径。判定压缩轨迹的左瓣/中心/右瓣拓扑、两侧纵向覆盖和闭合距离；允许左右任一瓣先画。单瓣抬笔会留下淡墨作为因果反馈，但第二个分开圆不能与前一笔拼成答案；中心摩擦和只在单侧累计长度也不能误过。键盘路线先以 Space 落笔，再用左右键让同一纸笔依次绕过可见两瓣并回到中心，不是方向密码。
- **信息层级**：未破解前不显示“档案八字”、数字 8、路线编号或操作步骤，移除随机环境图标。双瓣断墨、共用压痕、单瓣抬笔后留下的淡印与一笔完成后的连续墨色承担线索；无标准描摹模板、进度数字、中心箭头或完成按钮。桌面场景位于秒表左侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：318 / 318 通过，其中 6 项覆盖双瓣/共享中心结构、两个分开圆失败、中心摩擦与单侧累计失败、左右任一起笔的一笔拓扑和键盘纸笔路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标与浏览器原生连续触摸、单瓣保留、分笔失败、一笔双瓣成功，以及视口边界与核心区不遮挡。
  - 首轮截图目检发现抬笔后的临时折线与保留瓣印叠加，削弱了“闭合瓣被保留”的视觉事实；已只清除临时轨迹、保留平滑淡墨瓣印，并重新运行组件与双端浏览器验收。
  - 截图：`artifacts/screenshots/v2-sequential/069-desktop-1440.png`、`069-mobile-390.png`，均在左瓣单独完成并抬笔、淡墨保留但尚未连续完成右瓣时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从两个断口和共用压痕推理必须一笔连接；真实手机手指经过中央交叉时，较粗触点是否会让玩家误以为系统要求精确描线。
- **停止结论**：工程硬门已通过；不增加数字 8 轮廓、描摹箭头、中心穿越计数、笔画进度或“一笔画”明示，进入 070。

### 070 `twin-gates` — 工程验收通过

- **设计事实**：两张竖向纸层各有一道窄门和半条线路；直接并排时线路分离，把两门放在同一平面简单重合时也只能让一段半亮。真正答案是横移上层纸，使前门和后门按深度错位，让同一条线先穿前层、再从后层出来。
- **实现结果**：以专属前后纸层、两道拱形切口、两段半线、深度桥和纸封替换通用三纸堆叠。上层纸从侧置位置可宽松横向拖动；`0px` 是门洞平面对齐但线路仍半亮，`40–56px` 是前层在前、后门在后的连续通道并吸附到 `48px`。过度右移形成镜像错层，线路从后层先退出并保持断开；点击和向外拉远无效。左右键以 16px 可见步长移动同一纸层。
- **信息层级**：未破解前不显示“一条线穿两道门”或关卡名，移除随机环境图标。两张纸的色温、投影深度、不同横位的门洞和只在正确前后关系中亮起的连接线承担线索；无角色、钥匙、门锁、对齐箭头、坐标或进度条。桌面场景位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：324 / 324 通过，其中 6 项覆盖双层/双门/半线结构、点击与拉远失败、平面对齐半亮、正确深度连续、镜像错层失败和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标拖层、浏览器原生触摸拖层、平面对齐不通关、正确错层通关，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/070-desktop-1440.png`、`070-mobile-390.png`，均在两门平面对齐、只显出一扇叠合门且线路半亮但尚未建立前后通道时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从“对齐只能半亮”继续探索前后错层，而不是误以为交互失灵；真实手机横拖上层纸时，叠层投影是否足以表达前后关系。
- **停止结论**：工程硬门已通过；不增加深度编号、前后文字、对齐箭头、门洞计数或连续线路预览，进入 071。

### 071 `triple-actuator` — 工程验收通过

- **设计事实**：三枚纸质表盘由两条细齿带相连；转左盘只带动左带与中盘，转右盘只带动右带与中盘，唯有中盘能同时反向协调两侧。左右外盘各有一处浅槽，答案是让两枚影针同时进入宽槽，而不是依次校准三个表盘。
- **实现结果**：以专属三表盘、双齿带、双浅槽和纸封替换通用旋转控件。初态左/中/右角度为 `30° / 0° / 330°`；中盘移动 `+60°` 时两侧分别到达 `90° / 270°`。侧盘只点亮所属齿带且永不直接通关；只有中盘在松手、滚轮停顿 180ms 或键盘 Enter 时同时对槽才吸附锁定。点击以及把每个盘机械转满一圈均不得误过关。
- **信息层级**：未破解前不显示“中间的表盘”、关卡英文名、角度数字、方向箭头或操作步骤；材质差异、单侧齿带着色和两枚位置不同的浅槽承担因果线索。桌面场景位于秒表左侧，手机重排到主按钮下方，并移除随机环境图标。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：330 / 330 通过，其中 6 项覆盖三盘双带结构、侧盘单向传递、整圈机械枚举失败、中盘拖拽、滚轮停顿和键盘释放路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标拖盘、浏览器原生触摸拖盘、侧盘错误路径、中盘正确路径、视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/071-desktop-1440.png`、`071-mobile-390.png`，均在左侧齿带已响应但双槽尚未锁定时采集并人工检查；三盘关系清楚，未抢占秒表主层级。
- **仍缺证据**：未知答案玩家能否从“侧盘只影响一边、中盘影响两边”自然推导共享控制；真实手机连续横拖中盘的阻尼是否需要细调。
- **停止结论**：工程硬门已通过；不增加角度刻度、转向箭头、齿带标签、校准数值或显式“转中间”文案，进入 072。

### 072 `glass-relay-oscillator` — 工程验收通过

- **设计事实**：两片透明玻璃纸同频摆动，交会区域的双影反而更强；第三片带有不同方向的偏振纹，不是另一块普通玻璃。把它仅叠在交会处只能削弱双影，还必须通过自身折角转过约 90°，才会让相反纹理抵消摆动。
- **实现结果**：以专属双玻璃层、双影波纹、可自由拖放的偏振薄片及其同体折角替换通用三纸堆叠。偏振片初态与交会处错开；进入二维 `±24px` 宽容区后振幅从 `strong` 变为 `reduced`，但保持未锁定。拖动折角完成 `90°±15°` 的宽松转向后，双影归零并锁定；在交会区外旋转、单击、页面滚动和设备方向事件均不通关。
- **信息层级**：未破解前不显示“玻璃消振”、英文关名、角度数字、旋转图标或步骤说明。两块普通玻璃的同频双线、第三片不同材质的平行纹、覆盖后双线间距收窄和折角共同承担线索；减少动态模式以静态双线间距表达振幅。桌面场景位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：336 / 336 通过，其中 6 项覆盖三材质结构、仅叠放失败、离位旋转失败、拖放加折角转向、无关事件和键盘分步路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标和浏览器原生触摸执行离位转向失败、交会叠放减振、折角 90° 消振，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/072-desktop-1440.png`、`072-mobile-390.png`，均在偏振片已覆盖交会区、双影已减弱但尚未转向时采集并人工检查；透明层次和第三材质可辨识。
- **仍缺证据**：未知答案玩家能否把折角理解为可转动的同体结构，而不是装饰；不同手机屏幕亮度下三层透明材料的区分度。
- **停止结论**：工程硬门已通过；不增加旋转箭头、角度刻度、目标框、操作文案或“偏振”教学卡，进入 073。

### 073 `pressure-vault` — 工程验收通过

- **设计事实**：三枚软纸石外形、颜色和表面高光完全相同，但稳定按住时会压出大、小、中三种涟漪；三处纸槽自身的裁切面积和承载纹理则分别是中、大、小。必须先观察作用范围，再把石头放进容量相同的槽，不能按初始位置或颜色排序。
- **实现结果**：以专属三石、三承载槽、持续按压计时、二维拖放和槽纸形变替换通用影子排序。短点无效，稳定按住 320ms 才保留压力圈；未测量直接放置被拒绝。石头大于槽容量时槽面鼓起，小于容量时留下松边，正确容量才固定；映射为石 `0/1/2 → 槽 1/2/0`。键盘以 Space 测量、方向键选择可见槽、Enter 放置，保持同一“先测量再归槽”语义。
- **信息层级**：未破解前不显示关卡名、石头大小文字、槽编号、排序箭头或步骤说明。三石完全相同的实体外观、长按后不同直径的涟漪、三槽不同裁切面积，以及错误后的鼓包/松边承担双重证据；桌面场景位于秒表左侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：342 / 342 通过，其中 6 项覆盖同形石结构、短点无效、320ms 测量、未测量拒绝、过小/过大槽差异和三石键盘配对。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标长按/拖放与浏览器原生触摸长按/拖放、错误容量反馈、三组正确匹配，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/073-desktop-1440.png`、`073-mobile-390.png`，均在第一枚石头的大压力圈已显现并被放入过小槽后采集并人工检查；三枚实体仍保持同形，压力圈与槽容量关系可比较。
- **仍缺证据**：未知答案玩家是否会自然尝试长按而不是连续点击；真实手机长按后再抬手、重新拖动的两段动作是否显得冗长。
- **停止结论**：工程硬门已通过；不增加长按图标、尺寸文字、槽编号、目标高亮或自动吸引轨迹，进入 074。

### 074 `clue-relay-braid` — 工程验收通过

- **设计事实**：三条透明句带各只保留半句，单独阅读都不完整；每条边缘的纸粒分别偏向上、下、上。谜题不是把三层做六种全局前后排列，而是让每条带子在下、中、上三档中独立穿行；正确的上/下/上编织关系会令交会处逐词显出 `FOLLOW THE GAP / 沿着空隙`。
- **实现结果**：以专属三条半句透明带、三档穿行深度、边缘粒纹和交会清晰度替换通用叠层模板，形成 `3³ = 27` 种组合而非一次划过即可遍历的固定路线。竖向拖动分别改变每条带子的深度；正确一层只澄清一个词，三层同时正确才完成。单击、把三条带子统一上移/下移或机械遍历同一动作均不通关；键盘上下键在当前带上表达相同层级关系。
- **信息层级**：未破解前不显示“句子编织”、英文关名、完整答案、层级文字、上/下箭头、步骤或进度数字。三条半句、顶部/底部粒纹和层叠投影承担线索；桌面场景位于秒表右侧，手机重排到主按钮下方，均未遮挡标题、秒表或开始按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：348 / 348 通过，其中 6 项覆盖三带结构和边缘证据、点击/统一层级失败、错误下穿反馈、逐词清晰、机械遍历失败和键盘上/下/上路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标拖带、浏览器原生触摸拖带、错误层级、逐层清晰、最终编织，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/074-desktop-1440.png`、`074-mobile-390.png`，均在三条带子仍未完成编织时采集并人工检查；半句、粒纹与透明层级可比较，完整答案未提前泄露。
- **仍缺证据**：未知答案玩家能否在 20 秒内由上/下/上纸粒纹推断三条带子的独立穿行关系；中英文长度差异是否会让某一种语言的半句更容易拼读。
- **停止结论**：工程硬门已通过；不增加层级标签、方向箭头、完整句预览、组合计数或自动吸附轨迹，进入 075。

### 075 `relay-beacon-weave` — 工程验收通过

- **设计事实**：四条纸带已经完成交织，困难来自透明交会处无法判断谁在上，而不是要求玩家重新编织。侧边信标会让四处上下关系随阴影显形；纸轨边缘的一道实体刻痕给出唯一有依据的灯位，只有在那里四处才同时形成上/下/上/下并自动压实。
- **实现结果**：以专属固定四带编织、四交会阴影、单一共享信标、纸轨与 V 形刻痕替换通用“拖纸片到目标”。纸带不接收移动，点击或拖带均不改变编织；移动信标到中间等合理错误位置会显出两处相互矛盾的阴影，离开刻痕仍保持未锁定。信标进入刻痕的宽松 `±8%` 区域并松手才压实；左右方向键移动同一可见信标。
- **信息层级**：未破解前不显示关卡名、上/下文字、方向箭头、目标高亮或操作提示。固定纸带、随光改变的交会阴影、轨道和细小 V 形刻痕承担因果线索；桌面场景位于秒表左侧，手机重排到主按钮下方，均未遮挡挑战文案、秒表或按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：354 / 354 通过，其中 6 项覆盖固定四带/信标/刻痕结构、移动纸带失败、错误灯位矛盾、刻痕锁定、机械点击/反向扫光失败和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标和浏览器原生触摸移动信标、点击纸带无效、中间灯位矛盾、刻痕灯位锁定，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/075-desktop-1440.png`、`075-mobile-390.png`，均在信标已移动到错误中间位置、交会阴影出现矛盾但尚未压实时采集并人工检查；编织本体保持固定且不抢占秒表。
- **仍缺证据**：未知答案玩家能否在 10 秒内把纸轨刻痕理解为灯位而不是装饰；低亮度手机上交会阴影从模糊到矛盾的变化是否足够可察觉。
- **停止结论**：工程硬门已通过；不增加灯位箭头、轨道刻度、四处关系文字、显式目标光圈或重新编织步骤，进入 076。

### 076 `relay-quorum` — 工程验收通过

- **设计事实**：五枚纸叶正面为三开两闭，五个投影却为两开三闭；玩家需要同时比较正面、投影和每枚折痕，而不是只数一排。其中第三枚的正面方向与自身折痕相反，且只有把这一票单独翻转，正面与影子才会同时形成“闭”的多数。
- **实现结果**：以专属五纸叶、五投影、开/闭形态、独立折痕和翻面状态替换通用单按钮翻转。翻任一纸叶会同步改变其正面与投影；错误纸叶保留可逆状态供比较，不会自动替玩家纠正。完成条件严格为仅第三枚翻转的 `00100`，不是简单判断两个统计多数相同，因此把五枚依次各翻一次的 `11111` 不会在中途或最终误通关；Enter 翻转当前可见纸叶。
- **信息层级**：未破解前不显示关卡名、异常票、开闭计数、红绿状态或操作提示。纸叶展开/合拢的轮廓、投影宽窄和第三枚相反斜向折痕承担三层证据；桌面场景位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：360 / 360 通过，其中 6 项覆盖五叶/五影/折痕结构、错误票同步翻转、唯一票锁定、五票机械枚举失败、错误恢复和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证五枚各翻一次不通关、全部复位、错误票反馈、折痕异常票成功，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/076-desktop-1440.png`、`076-mobile-390.png`，均在第一枚错误纸叶翻转后采集并人工检查；纸叶、投影与折痕仍可横向比较，未显示答案文字。
- **仍缺证据**：未知答案玩家能否在 10 秒内注意到“正面多数与影子多数相反”并进一步检查折痕；小屏幕上五枚折痕方向的区分是否足够稳健。
- **停止结论**：工程硬门已通过；不增加票数、异常高亮、开闭标签、投票文字或逐枚错误提示，进入 077。

### 077 `split-operator` — 工程验收通过

- **设计事实**：纸面上的 `9.95 ≠ 10.00` 两组数字完全固定，真正可改的是 `≠` 的三道纸笔画。拿走斜杠只会留下 `=`，并不能完成；只有把斜杠移入右侧淡箭头折槽，共享纸机构才把两条横线压合成箭杆、斜笔画折成箭头，使“不等”变为“前往”。
- **实现结果**：以专属双数字组、两条固定横笔、可二维拖动斜笔、淡箭头纸槽和关系状态替换通用共享控制拖块。数字没有输入、删除或点击行为；单击斜杠无效，向左或竖向移走只显示 `=` 并保持未锁定。斜杠进入宽松二维槽并松手后，三笔重组为 `→`，纸纹预演减速；方向键移动同一可见斜笔。
- **信息层级**：未破解前不显示关卡名、“关系错误”、操作步骤、输入框或目标高亮。公式、可分离的纸笔画和低对比虚线箭头槽承担全部线索，且不依赖中英文语义；桌面场景位于秒表左侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：366 / 366 通过，其中 6 项覆盖固定数字/三笔/浅槽结构、数字不可编辑、移除斜杠仅成等号、箭头锁定、点击/竖拖失败和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标和浏览器原生触摸拖斜笔、点击失败、错误侧仅成等号、浅槽重组箭头，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/077-desktop-1440.png`、`077-mobile-390.png`，均在斜杠被移到左侧、等号显露但尚未形成箭头时采集并人工检查；公式清晰、槽位克制、手机数字不挤压。
- **仍缺证据**：未知答案玩家能否在 8 秒内由“数字都合理”转向操作关系符；淡箭头槽在不同显示器对比度下是否既可察觉又不过度泄题。
- **停止结论**：工程硬门已通过；不增加数字编辑、关系文字、拖动箭头、完成按钮或更强目标描边，进入 078。

### 078 `fourfold-oscillation` — 工程验收通过

- **设计事实**：四块半透明纸窗的外形与颜色一致，三块同相呼吸、一块反相；中央空槽的纸纹只与反相窗相合。拖动任一窗会在原位留下相位残影，错误同相窗进入中心会强化整体振幅，反相窗进入中心才与另外三块残影抵消。
- **实现结果**：以专属四窗、中心纸纹槽、同/反相呼吸、原位残影和二维拖放替换通用框拖动。单击四窗无效，所有窗使用同一颜色；错误窗进入宽松中心区会回弹并留下同相残影与强化反馈。第三块反相窗进入中心 `±12%` 区域才锁定并停止四窗振幅；把四窗机械地拖过同一非中心路线只留下四道轨迹，不会通关。Enter 把当前观察到的纸窗放入同一中心槽。
- **信息层级**：未破解前不显示关卡名、反相标签、颜色区别、位置编号、方向箭头或相位文字。呼吸方向、拖后残影和中央纹理承担线索；减少动态模式用三块张开轮廓与一块收拢轮廓表达相位。桌面场景位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：372 / 372 通过，其中 6 项覆盖同色四窗/中心槽/相位轨迹、点击与颜色猜测失败、错误同相窗强化、反相窗抵消、统一非中心遍历失败和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标和浏览器原生触摸拖窗、四窗点击无效、同相窗中心强化、反相窗中心抵消，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/078-desktop-1440.png`、`078-mobile-390.png`，均在错误同相窗进入中心后回弹、原位残影显现且尚未抵消时采集并人工检查；四窗与中心槽清楚，未抢占秒表。
- **仍缺证据**：未知答案玩家能否在 12 秒内辨认呼吸相位而不是按位置猜；不同设备刷新率与减少动态模式下，相位冗余轮廓是否同样易读。
- **停止结论**：工程硬门已通过；不增加反相文字、颜色编码、中心箭头、相位计数或自动高亮异常窗，进入 079。

### 079 `seven-relay-vote` — 工程验收通过

- **设计事实**：七枚投票纸叶中初始只露出三枚，折页背后留下四道与隐藏纸叶相配的投影；表面信息不足以判断多数。答案不是逐枚点击投票，而是调节同一折页，让七枚纸叶和四道背影在一个视野中同时成立，多数方向才会压下时间开关。
- **实现结果**：以专属七纸叶、四投影、单一可调折页、纸面开关和折页角度状态替换通用折叠按钮。逐枚点击七票完全无效；折页从闭合拖到半开时依次露出五票/两影，拖得过头会重新遮住一侧而只剩六票/三影，只有宽松的 55–88% 同时可见区才显示七票/四影并由四比三的右向多数自动压实。方向键移动同一可见折页，三次左键可进入相同宽容区。
- **信息层级**：未破解前不显示关卡名、多数字样、票数统计、拖动箭头、步骤或进度条。纸叶倾斜、折页遮挡、背面投影和浅压痕承担线索；桌面场景位于秒表左侧，手机重排到主按钮下方，均未遮挡核心游戏区。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：378 / 378 通过，其中 6 项覆盖七票/四影/折页结构、逐票点击失败、半开揭示、过折重新遮挡、宽松完整视野和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标与浏览器原生触摸折页、逐票点击无效、五票半开、六票过折、七票多数锁定，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/079-desktop-1440.png`、`079-mobile-390.png`，均在折页半开、五票/两影已显露但多数尚不完整时采集并人工检查；折页变化可察觉且没有提前写出答案。
- **仍缺证据**：未知答案玩家能否从三票与折后投影推断“改变折页而不是逐票操作”；低对比屏幕上四道背影是否足以与装饰纸纹区分。
- **停止结论**：工程硬门已通过；不增加票数、左右标签、折页箭头、进度百分比或逐票反馈，进入 080。

### 080 `pressure-singularity` — 工程验收通过

- **设计事实**：多层纸环被压向同一中心，直接按中心只会让纸层收得更紧；最外环边缘几乎不可见的外向纤维才是异常。答案是抓住最外环持续径向外拉，让张力依次穿过三层并保留松动，中心最终因压力释放自然形成空洞，而不是靠按压强度打开。
- **实现结果**：以专属四层同心纸环、六道外向纤维、受压中心、三段张力和自然空洞替换通用轨道纸片。点击中心会缩紧并留下可见压痕，但不会通关；点击或拖动内环完全无效。最外环一次外拉越过 20/42/64px 宽松阈值时，三层依次松开且保留；越过末段才释放。方向键在同一最外环上分三档表达等价径向外拉，不读取压力传感器。
- **信息层级**：未破解前不显示关卡名、“释放压力”、向外箭头、张力数值、步骤或进度条。环层压缩、中心按压后的进一步收紧、外缘纤维延伸和逐层松动承担因果；桌面位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：384 / 384 通过，其中 6 项覆盖四环/纤维/中心结构、中心按压错误反馈、内环无效、首层保留、单次连续三层释放和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标和浏览器原生触摸按中心、拖内环、外环半程与连续释放，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/080-desktop-1440.png`、`080-mobile-390.png`，均在中心已被错误按紧、外环又拉过首层阈值但尚未完全释放时采集并人工检查；环层与纤维可读且没有抢占秒表。
- **仍缺证据**：未知答案玩家能否把纤维理解成“由外释放”而不是装饰；小屏幕上最外环的可抓取带是否在不同手指尺寸下同样自然。
- **停止结论**：工程硬门已通过；不增加外拉箭头、压力读数、震动强度要求、中心禁用标志或显式阈值，进入 081。

### 081 `dual-device` — 工程验收通过

- **设计事实**：两枚纸半环共享一个中央插口，但左半带指针/单指纹，右半带键帽/双触点纹；同一种输入无法填满整个插口。桌面必须由真实指针把左半送入、键盘把右半送入；手机则由单指与双指分别承担两半，不能靠输入口令或重复点击替代。
- **实现结果**：以专属双色纸半环、共享虚线插口、指针纹、响应式键帽/双触点纹和两种输入状态替换通用耦合拖块。点击两半无效，鼠标拖右半或手机单指触右半只显出材质不匹配；左半只接受鼠标/单指向中心拖，右半桌面只接受左方向键、手机只在同一半环检测到双触点后接受向中心移动。两半顺序独立，只有都归位才合成完整纸环。
- **信息层级**：未破解前不显示关卡名、“指针＋键盘”答案、输入步骤、模式标签或完成计数。缺半圆轮廓、两种表面纹理和共同插口承担线索；移动断点隐藏键帽纹并显示双触点纹，避免无物理键盘设备出现错误语义。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：390 / 390 通过，其中 6 项覆盖双半环/共享插口/响应式纹理、点击与同输入失败、指针单半、键盘单半、桌面组合和手机单指＋双指组合。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标、键盘和 CDP 原生单点/双点触摸，移动端键帽隐藏，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/081-desktop-1440.png`、`081-mobile-390.png`，均在指针/单指半环已进入共享插口、另一半仍等待不同输入时采集并人工检查；桌面显示键帽纹，手机显示双触点纹。
- **仍缺证据**：未知答案玩家能否从两种材质理解“各用一种输入”而非把右半当作不可操作装饰；部分移动浏览器的双指手势是否会与页面缩放竞争。
- **停止结论**：工程硬门已通过；不增加输入名称、操作动画教程、口令后门、设备检测弹窗或第三种输入模式，进入 082。

### 082 `pointer-majority` — 工程验收通过

- **设计事实**：一个实心纸指针周围有三枚旧箭头影和两个浅窝；两枚影子指向同一窝，第三枚指向另一窝。答案不是依次试遍两个候选，也不是看颜色，而是把实心指针送到两枚纸影共同支持的位置；靠近候选时，对应影子会缩短，令“谁与谁同意”继续可观察。
- **实现结果**：以专属实心指针、三枚同色同形箭头影、两处浅窝、实时影长和候选状态替换通用共享控制拖块。点击指针和浅窝无效；拖到少数窝只缩短第三影并保留未锁定反馈，离开两窝回到中间仍不会按访问次数累计。只有进入两枚箭头共同指向的宽松浅窝才缩短前两影并合并。方向键在同一实心指针上选择候选，右键先试少数不会阻止随后左键按多数完成。
- **信息层级**：未破解前不显示关卡名、“多数目标”、票数、颜色编码、候选标签、计数或步骤。箭头方向、同色轮廓、靠近后的长度变化和浅窝压痕承担全部线索；桌面位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：396 / 396 通过，其中 6 项覆盖指针/三影/双窝结构、点击失败、少数影反馈、多数合并、访问计数不可利用和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证三影计算色一致、真实鼠标/原生触摸先访少数、回到两窝之间、再访多数，以及视口边界与核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/082-desktop-1440.png`、`082-mobile-390.png`，均在实心指针误入少数窝、第三影缩短而前两影仍共同指向另一窝时采集并人工检查；多数关系可见但没有文字答案。
- **仍缺证据**：未知答案玩家能否把箭头影理解成旧意见而非普通装饰；低对比显示器下“影子缩短”是否足以作为错误候选反馈。
- **停止结论**：工程硬门已通过；不增加 2:1 数字、颜色区分、多数窝高亮、箭头标签或访问次数奖励，进入 083。

### 083 `alternating-target` — 工程验收通过

- **设计事实**：两枚目标纸环分居交叉轨迹两端，触及当前纸环会让它消失并在另一侧出现；每次交换都会在两条轨迹共同经过的中央空白留下极浅压痕。答案不是继续追逐某一端，而是在至少看清两次交换后，于两环都暂时离场的 1.2 秒空窗触及共同中心。
- **实现结果**：以专属双端纸环、两条交叉虚线轨迹、逐次保留的中央压痕和 1200ms 空窗替换通用点击目标。直接触及中心、只追一次后触及中心、或在空窗关闭后再触及都不会完成；第二次及以后追逐会打开同一宽松空窗，中心触点仍保持不发光。键盘以聚焦纸环后 Enter 追逐、空窗内 Space 触及中心，表达同一空间关系。
- **信息层级**：未破解前不显示关卡名、追逐次数、中心标签、倒计时、目标箭头或步骤。纸环交替缺席、两条轨迹重叠和逐次加深但仍克制的中央压痕承担线索；桌面位于秒表右侧，手机重排到主按钮下方，核心计时区保持无遮挡。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：402 / 402 通过，其中 6 项覆盖双目标/共同中心结构、过早触及失败、单次追逐不足、重复追逐不直接完成、1.2 秒空窗成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标和浏览器原生触摸的过早中心、一次追逐、两次追逐与空窗中心触发，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/083-desktop-1440.png`、`083-mobile-390.png`，均在纸环交替、交叉轨迹与中央压痕可见但中心没有发光提示时采集并人工检查；谜题保持次要层级。
- **仍缺证据**：未知答案玩家能否在不过度追逐的情况下，把中央压痕理解为两个目标共享的位置；真实低刷新率移动设备上 1.2 秒空窗是否仍显得从容。
- **停止结论**：工程硬门已通过；不增加中心高亮、追逐计数、倒计时、交换箭头或“等它消失”文案，进入 084。

### 084 `ghost-session` — 工程验收通过

- **设计事实**：一枚可移动纸点位于两个互补半圆压痕之间，没有普通匹配槽。玩家先把纸点放到任一有依据的半圆，再正常开始、停止并使用“再来一次”；纸点回到起点，但该位置的浅影只在当前关卡会话保留。第二次把纸点放到互补半圆，旧影与新位置才合成完整圆。
- **实现结果**：以专属纸点、左右互补压痕、会话浅影和真实回合重置状态替换通用盖页按钮。未重置前第一位置只记录候选，不显影也不完成；偏离两处压痕的移动会清空候选；重置后重复同侧仍不完成，只有互补侧锁定。父级仅在当前 `ghost-session` 连续重试时保留锚点与重置序号，进入其他关卡即清除；键盘左右方向键操作同一纸点。
- **信息层级**：未破解前不显示关卡名、“完整圆”、重置次数、步骤或目标高亮。两个相向半圆、纸点移动后短暂出现的页边小字以及重置后只在一侧加深的旧影承担线索；桌面位于秒表右侧，手机重排到主按钮下方，结果层退出后才采集视觉证据。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：408 / 408 通过，其中 6 项覆盖专属结构、重置前不显影、无依据移动失败、一次重置保留、同侧重复失败/互补成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标/原生触摸先落左压痕、正常开始与停止一局、普通“Run again”重置、旧影保留、同侧失败和互补侧成功，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/084-desktop-1440.png`、`084-mobile-390.png`，均在一次普通重置后纸点已回到中央、左侧旧影仍保留时采集并人工检查；退出动画残影已排除。
- **仍缺证据**：未知答案玩家能否把两个半圆理解为需要跨一次重置完成的同一形状；真实网络延迟与结果页停留时间较长时，玩家是否仍能记住第一次位置。
- **停止结论**：工程硬门已通过；不增加“先放左边”文案、重置专用按钮、历史列表、位置编号或常驻进度，进入 085。

### 085 `phase-return` — 工程验收通过

- **设计事实**：一枚月相纸盘在前景持续移动；向下拉页面盖纸后，主盘被遮住，但盖纸下缘的小窗仍显示背景月相继续变化。玩家需在小窗中的相位与纸面半圆刻痕互补时揭开，二者才合成满圆；看不见不等于停止。
- **实现结果**：以专属月相盘、互补半圆刻痕、可上下拖动的正式盖纸、下缘相位窗和错误相位差影替换通用盖/揭按钮。点击月盘或盖纸无效；过早揭开保留可撤回差影，重新盖住后相位从窗口继续可见。互补接受区为 1800–3600ms，共 1.8 秒，不做毫秒反应；方向键路线改为同一盖纸上的 Enter/Space 盖住与揭开。合页手柄全程保持在裁切范围内，手机可稳定抓取。
- **信息层级**：未破解前不显示关卡名、互补相位、倒计时、角度或揭开提示。月盘明暗边、半圆刻痕、遮挡后仍移动的小窗月相以及过早揭开的差影承担因果；桌面位于秒表左侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：414 / 414 通过，其中 6 项覆盖专属结构、初始 visibility 无效、点击无效、过早揭开差影、1.8 秒宽窗成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标/原生触摸盖住、700ms 过早揭开、重新盖住、窗口内相位继续与 2.1 秒揭开成功，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/085-desktop-1440.png`、`085-mobile-390.png`，均在页面完全盖住、下缘小窗仍显示互补月相时采集并人工检查；主计时器仍为第一视觉。
- **仍缺证据**：未知答案玩家能否由小窗变化推断“被盖住仍继续”，而不是把窗口当作纯装饰；不同刷新率设备上的月相位移动是否同样自然。
- **停止结论**：工程硬门已通过；不增加等待秒数、相位标签、揭开箭头、目标高亮或真实标签页唯一路线，进入 086。

### 086 `eclipse-session` — 工程验收通过

- **设计事实**：右上真实菜单打开后，其纸张多出一个可随临时纸层移动的圆形缺口；页面太阳位于缺口可到达的邻近位置。玩家需把缺口套住太阳形成日冕，再关闭真实菜单，缺口携带的圆影才落到秒表小数点。
- **实现结果**：为 `eclipse-session` 增加专属页面太阳、日冕关系和小数点圆影，同时把可拖纸层嵌入真实设置抽屉。仅开关菜单记录失败闭合，不会通关；预先存在的偏移也不能绕过“菜单打开时形成日食”的因果。纸层手柄支持真实鼠标、原生触摸与三次 ArrowUp 宽松档位，目标区为 60–84px；关闭时才锁定并搬运影子。纸层本身不接管设置内容点击。
- **信息层级**：未破解前不显示关卡名、“日食”、目标偏移、拖动步骤或关闭提示。菜单纸纹、左缘小纸舌、圆形缺口、正确套合时的明亮日冕和关闭后的圆影承担线索；真实语言、模式、难度、提示、档案、排行与进度功能均保持可见可用。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：420 / 420 通过，其中 6 项覆盖专属结构、普通开关失败、预载偏移不可绕过、菜单内日冕、关闭后搬影和键盘纸层路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实菜单开关失败、提示设置仍可点击、真实鼠标/原生触摸移动菜单纸层、日冕形成与关闭后小数点圆影，以及页面场景边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/086-desktop-1440.png`、`086-mobile-390.png`，均在真实菜单打开、设置内容完整可读、纸层缺口已套住太阳时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把左缘纸舌与圆缺口理解为同一可移动纸层；手机全屏菜单里日冕与普通装饰圆环的区分是否足够明确。
- **停止结论**：工程硬门已通过；不把菜单项改成谜题按钮，不隐藏设置，不增加“拖菜单”文案、坐标读数或关闭倒计时，进入 087。

### 087 `triple-gravity` — 工程验收通过

- **设计事实**：一张独立纸框悬着三颗重力珠，纸面只有三个相互独立的重力槽和一条中段断开的浅 U 形凹痕。玩家需要依次让纸框朝下、朝右、朝上偏转；三次方向证据会留在纸面并接成完整 U 形，而不是把某颗珠子拖进目标点。
- **实现结果**：以专属可旋转纸框、三颗悬珠、三个重力槽、断裂 U 槽、保留轨迹和可撤回错误影线替换固定路线移动。单击纸框和伪造设备方向事件不会完成；错误首方向会收回当前证据并留下短暂影线，正确的下→右两步也不会提前完成，只有下→右→上三段关系成立才锁定。鼠标与原生触摸均以同一纸框拖拽方向操作，键盘以 ArrowDown→ArrowRight→ArrowUp 表达相同证据序列。
- **信息层级**：未破解前不显示关卡名、方向答案、槽位编号、箭头、步骤数或设备姿态读数。三颗珠子的悬线、三类浅槽、逐段保留的纸面轨迹与错误后自动收回的影线承担发现和纠错；桌面位于秒表右侧，手机重排到主按钮下方，计时器与主按钮无遮挡。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：426 / 426 通过，其中 6 项覆盖专属结构、点击/方向事件不可绕过、错误首步回退、两步不提前完成、三段成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标/浏览器原生触摸的错误右移、正确下→右→上、保留轨迹、成功锁定，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/087-desktop-1440.png`、`087-mobile-390.png`，均在前两段正确重力轨迹已保留、第三段尚未完成时采集并人工检查；谜题保持次要视觉层级。
- **仍缺证据**：未知答案玩家能否从三种悬线和断裂 U 槽推断需要改变整个纸框的重力方向；不同触摸设备上斜向手势是否都能自然归入四个宽松方向。
- **停止结论**：工程硬门已通过；不增加方向箭头、序列文案、进度计数、真实陀螺仪唯一解或珠子拖拽目标，进入 088。

### 088 `liminal-device` — 工程验收通过

- **设计事实**：写着“内”与“外”的两张纸片分居纸面两侧，中央双面折页和左右不同纹理共同定义当前边界。普通穿越会从另一侧回来但找不到座位；先把折页翻到背面，内外位置和边缘纹理一起交换，再把此时位于新外侧的“外”纸片送出，它才会从新定义的内侧回返并落入唯一空槽。
- **实现结果**：以专属内/外纸片、居中双面折页、两种边缘纹理、回返轨迹和空槽替换通用折纸按钮。折页前穿越“外”只形成未落座回返；折页后穿越“内”仍不能完成；只有背面状态下“外”越过新外边缘才锁定。折页支持直接翻开或水平翻动，纸片使用一次宽松横向越界拖动；键盘以折页 Enter 加“外”纸片 ArrowLeft 表达同一两步关系。修复了 3D 折页以左边为轴时偏移并覆盖纸片的问题，并抑制拖折后浏览器合成 click 导致的重复翻面。
- **信息层级**：未破解前不显示关卡名、先后步骤、出口箭头、边界说明、浏览器导航或进度数字。内/外字样、折页正反材质、左右边缘纹理交换、未落座回返曲线与空槽承担推理；桌面位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：432 / 432 通过，其中 6 项覆盖专属结构、旧边界回返失败、折页改写内外、错误纸片失败、正确两步成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标/浏览器原生触摸的折页前错误穿越、翻面、内侧纸片错误穿越、外侧纸片正确回返落槽，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/088-desktop-1440.png`、`088-mobile-390.png`，均在折页已翻面、内外换位且错误纸片刚完成未落座回返时采集并人工检查；中央折页不再误拦截两侧纸片。
- **仍缺证据**：未知答案玩家能否把边缘纹理交换理解成内外定义改变，而不是单纯视觉换色；英文 `IN/OUT` 和中文“内/外”在首次观察中是否具有相近难度。
- **停止结论**：工程硬门已通过；不增加“先翻再拖”文案、出口高亮、浏览器后退动作、语言切换依赖或第三种操作模式，进入 089。

### 089 `device-braid` — 工程验收通过

- **设计事实**：同一条纸纹丝带在内嵌纸视口的宽版中横向连续、在窄版中纵向点状重排，两种布局各自缺少完整交叉关系。玩家必须操作纸内视口而非浏览器：先收窄，让点状层经过中央纸夹并将其夹住，再展开，连续层才从保留点状层下方穿过形成一次编织。
- **实现结果**：以专属可调纸视口、宽窄折痕重排、连续/点状双形态丝带、中央纸夹与宽度手柄替换通用 resize 拖块。真实窗口 resize 和方向事件均不改变关卡状态；宽版夹纸只产生空夹反馈；未保留点状层的窄→宽只恢复分离状态。宽松拖动跨过 55px 即即时切换宽窄档位，窄版夹住后再展开才锁定 `dotted-over-continuous` 层级；键盘以手柄 ArrowLeft、纸夹 Enter、手柄 ArrowRight 完成同一关系。实机发现桌面手柄曾落入计时器透明布局盒，已通过仅收窄/左移本关桌面纸框修复，手机尺寸不变，并加入最上层命中断言。
- **信息层级**：未破解前不显示关卡名、宽窄数值、步骤、交叉答案或真实窗口提示。视口边缘、响应折痕、宽版连续带、窄版点状带、纸夹留痕和编织层级承担因果；桌面位于秒表左侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：438 / 438 通过，其中 6 项覆盖专属结构、真实 resize 不可绕过、宽版空夹、未保留往返失败、正确编织和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实 viewport resize、空夹、无保留窄→宽、窄版夹持、展开编织、桌面命中层级，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/089-desktop-1440.png`、`089-mobile-390.png`，均在窄版点状层已被纸夹保留、尚未展开时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把宽度手柄和纸夹理解成需要组合使用，而不是把其中一个视为装饰；真实低性能手机上的折痕重排是否仍清晰连贯。
- **停止结论**：工程硬门已通过；不绑定语言切换、真实窗口 resize 或设备方向，不增加宽度读数、层级文字、步骤提示或第三种纸带，进入 090。

### 090 `archive-labyrinth` — 工程验收通过

- **设计事实**：六个档案浅格分散在纸面四周，单独打开没有路线；唯一可移动的透明地图窗携带三段连续曲线，覆盖不同纸纹时只会显出局部或死端。玩家需要搬动整张地图，而不是依次检查格子，找到让三段从起点连续到终点的唯一覆盖位置。
- **实现结果**：为第 090 关新增专属六格档案纸纹、透明地图窗、三段路线、局部显墨和死端反馈，没有复用第 043 关的“连续注意力路线”。初始地图有意覆盖一格；玩家可先打开其余格，再移动地图露出被盖格，但即使六格全部打开也不会完成。地图拖到合理但不完整的位置只显示 1–2 段死端；进入宽松唯一覆盖区才三段连续并锁定。键盘用三次 ArrowRight、两次 ArrowUp移动同一地图；答案不依赖日期。桌面实机同时修复了最左档案格落入计时器透明布局盒的问题，将专属纸面右移 16px且保持视口内。
- **信息层级**：未破解前不显示关卡名、地图坐标、唯一位置、路线计数、档案顺序或日期。档案纸纹、地图内三段淡线、覆盖时逐段显墨和错误位置的珊瑚色死端承担推理；桌面位于秒表右侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：444 / 444 通过，其中 6 项覆盖专属结构、依次开格失败、错误覆盖死端、唯一覆盖成功、日期无关和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证六格真实点击（含先被地图覆盖、移动后露出的格）、错误覆盖、三段唯一连接、真实鼠标/原生触摸、命中层级、视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/090-desktop-1440.png`、`090-mobile-390.png`，均在地图移到错误覆盖并显出死端、六格均已尝试但尚未通关时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把地图中的曲线与档案纸纹建立关系，而不是继续尝试打开格子；三段淡线在不同屏幕亮度下是否足够可见但不过度提示。
- **停止结论**：工程硬门已通过；不增加路线箭头、正确格高亮、地图坐标、日期彩蛋或档案点击序列，进入 091。

### 091 `mode-flip` — 工程验收通过

- **设计事实**：正常与缓慢两张模式牌位于同一张空白衬纸之上，翻任一牌都会带动另一牌反向翻面，局部看似变化但整页节奏始终不变。真正的共同控制物不是两张写有模式的牌，而是它们背后的同一张空白衬纸；向上掀起衬纸后，大面积慢影才覆盖两牌并改变整页节奏。
- **实现结果**：以专属双模式牌、耦合翻面、共用空白衬纸、慢影覆盖和页面节奏状态替换通用折叠按钮。任一模式牌都会同时翻转两牌，连续十次也只增加局部翻面次数，不会改变 `page-speed=normal`；单击空白衬纸无效，必须从衬纸露出的下缘向上拖动至少 50px。键盘在同一衬纸上按 Enter 完成语义等价的最终动作；没有调用真实设置或模式切换。
- **信息层级**：未破解前不显示关卡名、“衬纸”答案、翻面次数、步骤、模式说明或设置入口。两牌相反正背面、共同底纸的露出边缘和成功后的大面积慢影承担推理；桌面位于秒表左侧，手机重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：450 / 450 通过，其中 6 项覆盖专属结构、耦合抵消、十次翻牌不完成、单击衬纸失败、上掀衬纸成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实卡牌点击、四次抵消、衬纸单击无效、真实鼠标/原生触摸上掀、整页慢态，以及视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/091-desktop-1440.png`、`091-mobile-390.png`，均在多次模式牌互相抵消、衬纸尚未掀起时采集并人工检查。
- **仍缺证据**：未知答案玩家能否注意到两牌共用的空白底纸，而不是把反复翻牌当作纯动画；背面文字镜像在不同语言中是否都能清楚传达“局部翻转”。
- **停止结论**：工程硬门已通过；不增加“拖衬纸”文案、真实设置面板、翻牌次数奖励、模式开关或第二个最终动作，进入 092。

### 092 `five-finger-echo` — 工程验收通过

- **设计事实**：五个空心纸环共享一个几何圆心；触碰离圆心越远，只会穿过 1–4 层并留下对应数量的短回声。答案不是“五指同时按”或“连续点五次”，而是找到五层真正共享的位置并只触碰一次。
- **实现结果**：以专属五层同心纸环、无发光圆心、按深度返回的短波和单触点命中判定替换通用拖拽组件。任意五次离心触碰只累计尝试次数，不能把次数伪装成穿层数；圆心单击/单指触摸一次即穿过五层并锁定。键盘在整片回声场按 Enter/Space 执行语义等价的圆心触碰，不要求真实五点触控。
- **信息层级**：未破解前不显示关卡名、共同圆心答案、五次点击说明或发光靶点；只由五层纸环的真实重叠关系和每次触碰产生的 1–4 道短波承担推理线索。桌面场景位于秒表右侧，手机缩小后重排到主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：456 / 456 通过，其中 6 项覆盖五层结构、离心回声、五次乱点失败、逐层短波、共同圆心成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证五次真实鼠标/原生触摸乱点不通关、一次圆心触碰产生五道回声并通关、视口边界、隐藏答案、无随机环境标记及核心区不遮挡。
  - 首轮手机验收发现 336px 圆形纸场无法同时避开主按钮并留在 844px 视口；将移动端纸场缩至 308px 后重跑双端通过，没有放宽边界断言。
  - 截图：`artifacts/screenshots/v2-sequential/092-desktop-1440.png`、`092-mobile-390.png`，均在第五次离心触碰后、尚未命中共同圆心时采集并人工检查。
- **仍缺证据**：未知答案玩家是否能从“不同位置回声层数不同”自然归纳共同圆心，以及真实手机上 308px 纸场的中心命中是否既可控又不过度精确。
- **停止结论**：工程硬门已通过；不增加圆心高亮、五指图标、层数数字、触碰轨迹、连续点击奖励或文字答案，进入 093。

### 093 `six-beat-lock` — 工程验收通过

- **设计事实**：六段长短相间纸带围绕共同端点摊开，每段中点都有真实折线；它看似节拍谱，但六次敲击不是答案。沿中点把每段外半折回后，外端点会逐一落在共同中心，最终由六拍形成一枚厚实单拍印章。
- **实现结果**：以专属六向纸带、长短交替长度、中点虚折线、可翻折外半、端点影子与共享印章替换通用折叠按钮。点击纸带完全不推进；拖动外端远离中心会产生回弹；只有把外端宽松拖回共享中心才保留该折叠。六段可按任意顺序完成，键盘在各段按 Enter/Space 执行等价折叠。
- **信息层级**：未破解前不显示关卡名、节奏指令、折叠步骤或共同端点答案；长短差、六条中线、折回后端点重合与中心逐层变厚承担推理。桌面纸场位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：462 / 462 通过，其中 6 项覆盖长短交替结构、六次点击失败、反向回弹、任意折序保留、六端汇合和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证六次真实点击无效、错误方向回弹、乱序真实鼠标/原生触摸折合、44px 操作面、共享印章锁定、视口边界及核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/093-desktop-1440.png`、`093-mobile-390.png`，均在三段乱序折合并保留、其余端点仍展开时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把长短纸带和中点折线归纳为“压缩时间距离”而不是继续尝试节拍；真实手机单指从不同角度把外端拖向中心时的手指遮挡程度。
- **停止结论**：工程硬门已通过；不增加折叠箭头、段编号、摩斯知识、完成计数、中心高亮或固定折序，进入 094。

### 094 `beacon-saturation` — 工程验收通过

- **设计事实**：四层彩纸在中心叠出最浓颜色，也恰好把钟面结构淹没；每层同时拥有斜纹、格纹、点纹或同心槽，因此答案不能只靠辨色。白色遮光纸移动到中心会减去刺激而非再加颜色，纹理钟面随饱和度降低逐步显现。
- **实现结果**：以专属四层混色纹理纸、隐藏钟面、可拖白色遮光片和连续去饱和反馈替换通用光点拖拽。点击彩层无效；遮光片停在外围仍遮蔽钟面，接近中心只显出部分纹理，进入宽松中心区才清除饱和并锁定。方向键按三次右、两次上到达同一几何位置；全程无闪烁。
- **信息层级**：未破解前不显示关卡名、颜色名称、中心标记或“去饱和”答案；彩纸重叠越深越难读、四种独立纹理、白片经过区域的纹理增强承担推理。桌面场景位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：468 / 468 通过，其中 6 项覆盖独立纹理、彩层点击失败、接近时显纹、偏离中心失败、中心去色成功和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证四种纹理、非颜色唯一、无闪烁、真实鼠标/原生触摸的偏离/接近/中心三态、44px 白片操作面、视口边界和核心区不遮挡。
  - 手机快速连续拖动曾因 80ms 位置过渡造成视觉坐标与状态坐标短暂错位，第二次松手被误判中心；已保存最后真实移动点并取消白片位置插值，重跑组件与双端浏览器验收通过。
  - 截图：`artifacts/screenshots/v2-sequential/094-desktop-1440.png`、`094-mobile-390.png`，均在白片接近中心、纹理开始显现但钟面尚未锁定时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把“颜色越浓信息越少”归纳为减色而不是继续移动彩层；色觉差异玩家对四类线纹的真实辨识效果仍需人工验证。
- **停止结论**：工程硬门已通过；不增加颜色名称、中心光圈、饱和度数字、目标箭头、闪烁扫描线或第二块遮光片，进入 095。

### 095 `triple-phase` — 工程验收通过

- **设计事实**：过去、现在、未来三张半透明时间片各带同一圆环的一段缺口，边缘分别压有一、二、三道短痕。重叠能补足形状，但只有按时间先后决定纸层深度，才不会出现错位重影。
- **实现结果**：以专属三张半透明时间片、互补缺口、边缘压痕、共享叠放井和重影层替换通用层叠模板。单击三张纸片不推进；任意错误叠序都会保留 1–2 层重影并保持开放。纸片可从叠放井最上层逐张拖出，自动回到各自起点，避免错误尝试后互相遮挡而无法重排；过去→现在→未来才形成单环并锁定。键盘在每张纸片上按 ArrowUp 叠入、ArrowDown 撤回。
- **信息层级**：未破解前不显示关卡名、过去/现在/未来文字答案、步骤、目标顺序或完整环提示；三种缺口角度、1/2/3 道边缘压痕和错误层序的重影承担观察与推理。桌面场景位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：474 / 474 通过，其中 6 项覆盖专属结构、点击失败、错误层序、正确相邻层、完整顺序和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标/浏览器原生触摸错误叠序、逐层撤回、自动归位、正确重组、44px 操作面、视口边界和核心区不遮挡。
  - 首轮浏览器验收发现错误叠放后撤出的三张纸会停在同一位置并互相遮挡；已改为撤出即归位各自起点，双端重跑通过。
  - 截图：`artifacts/screenshots/v2-sequential/095-desktop-1440.png`、`095-mobile-390.png`，均在三层错误叠序产生重影、尚未通关时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从边缘短痕归纳时间层序，以及半透明叠层在不同屏幕亮度下是否仍能清晰辨认重影差异。
- **停止结论**：工程硬门已通过；不增加层序文字、数字步骤、吸附箭头、时间标签、中心高亮或旋转精调，进入 096。

### 096 `seven-beat-null` — 工程验收通过

- **设计事实**：七枚墨点沿纸带向零点收拢，正常间距逐次减半；第五枚插得过近，使它后方的距离反而突然变大。异常必须由前后两段距离共同判断，而不是从颜色或编号猜测。
- **实现结果**：以专属七点收敛纸带、零点、前后距离纤维和断裂曲线替换通用阴影分类器。点击无效；移动任一点会显出其两侧距离，错误点拖出后回弹。为满足“逐点试删不能一次碰巧成功”，直接把未观察的点拖出只会回弹并留下观察状态；只有先检查异常点、再作第二次明确移除，剩余六点才恢复平滑减半并锁定。键盘以方向键观察、Delete/Backspace 移除。
- **信息层级**：未破解前不显示关卡名、点编号、异常点、数学比值或删除指令；点距逐级压缩、异常处“极短后突然变长”、移动后的双侧纤维和错误回弹承担推理。桌面场景位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：480 / 480 通过，其中 6 项覆盖七点结构、点击/未观察删除失败、错误回弹、一次机械遍历失败、正确移除和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实鼠标/浏览器原生触摸检查错误点、错误回弹、未观察异常点直接移除失败、二次有意移除成功、44px 操作面、视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/096-desktop-1440.png`、`096-mobile-390.png`，均在异常点已被直接试删并回弹、两侧距离关系可观察但尚未通关时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把非线性压缩看成“逐次减半”而不是普通透视，以及两段细纤维在低亮度手机上是否足够可辨。
- **停止结论**：工程硬门已通过；不增加数字比值、异常点着色、删除图标、逐点编号、永久试删进度或数学教程，进入 097。

### 097 `sevenfold-ack` — 工程验收通过

- **设计事实**：七张折页共用一根竖直书脊，逐页翻动只会彼此覆盖；书脊末端唯一的纸扣能按共同折痕依次抬起七页，且只有落在压痕位置时七道页影同时指向中心。
- **实现结果**：以专属七折页、共用书脊、可拖纸扣、唯一压痕和七道共享影线替换通用双物拖动。点击七页只记录局部翻页并保持遮盖；短拉纸扣只抬起部分页面，拉过压痕会让影线外散。纸扣在宽松刻痕区落定时七页全部抬起并完成共同回应。键盘在同一纸扣上用 ArrowDown/ArrowUp 分档移动。
- **信息层级**：未破解前不显示关卡名、页数说明、拉动指令、刻痕数值或完成方向；共同书脊、纸扣、局部抬页、椭圆压痕和同步影线承担推理。桌面场景位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：486 / 486 通过，其中 6 项覆盖专属结构、七次翻页失败、短拉、越过刻痕、正确共享拉动和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证七页真实点击、真实鼠标/浏览器原生触摸的部分抬升、越界错位、回拉到刻痕、48px 纸扣、视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/097-desktop-1440.png`、`097-mobile-390.png`，均在七页被共同纸扣拉过压痕、影线尚未汇聚时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从共享书脊自然转向纸扣而不是继续点页，以及七道淡影在真实低对比度屏幕上的可见性。
- **停止结论**：工程硬门已通过；不增加拉动箭头、页面编号、共享控制文字、完成百分比、声效计数或快速连点路线，进入 098。

### 098 `quad-phase` — 工程验收通过

- **设计事实**：四张纸片分别记录 `2.50 / 5.00 / 7.50 / 10.00`，被打乱在四个象限；四片受中央十字机械联动，不能单独搬动。只有一个离散档位让四个时刻沿顺时针持续增加，并让象限边缘路径闭合。
- **实现结果**：以专属四象限时间纸片、中央联动十字、四档排列和按正确相邻关系保留的边缘路径替换通用旋转控件。点击或拖动单张时间片完全不改变顺序；中央十字每次只进入 90° 离散状态，错误档保留 0–1 段局部路径，第二档形成 `0,1,2,3` 的完整循环。支持真实拖动、滚轮档位和左右方向键；所有时刻严格显示两位小数。
- **信息层级**：未破解前不显示关卡名、顺时针答案、档位数字或排序步骤；四个可读时刻、共同十字、局部边线与闭环承担推理。桌面场景位于秒表右侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：492 / 492 通过，其中 6 项覆盖专属结构、单片操作失败、错误档、正确闭环、滚轮和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证四片真实点击不改变顺序、真实鼠标/浏览器原生触摸的错误一档与正确二档、44px 操作面、两位小数、视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/098-desktop-1440.png`、`098-mobile-390.png`，均在第一错误档只保留一段相邻路径、尚未闭环时采集并人工检查。
- **仍缺证据**：未知答案玩家能否从两位小数时刻读出顺时针排序，而不是把中央十字当成装饰；移动端四片紧凑布局是否需要真人确认阅读舒适度。
- **停止结论**：工程硬门已通过；不增加顺序箭头、象限编号、拖动轨迹、自动排序、档位指示或逐片控制，进入 099。

### 099 `relay-polyrhythm` — 工程验收通过

- **设计事实**：二分纸波与三分纸波叠在同一水平区域，单看各自都不能给出答案；一整条六孔透明检查带必须同时覆盖两条波的共同交会位置，显出二拍与三拍共享的六点周期。
- **实现结果**：以专属两条不同纹理纸波、六个底层交会点、一体式透明检查带和六孔同步偏差反馈替换通用波形拖条。点击两条波或按 2/3/6 次检查带都不推进；检查带移到无关位置保持分离，接近时三个孔先显出同步缩差，整带进入宽松共同位置才六孔同时对齐。键盘用三次 ArrowRight 到达同一位置；不读取麦克风、不要求听觉或复节奏表演。
- **信息层级**：未破解前不显示关卡名、二/三/六数字说明、交点答案或拖动目标；两种波形密度、六个空孔、底层淡交点和同步收差承担推理。桌面场景位于秒表左侧，手机位于主按钮下方。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：498 / 498 通过，其中 6 项覆盖专属结构、次数点击失败、无关位置、接近反馈、整体对齐和键盘等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证两条波/检查带真实点击不推进、真实鼠标/浏览器原生触摸的无关/接近/整体对齐三态、44px 检查带、无音频依赖、视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/099-desktop-1440.png`、`099-mobile-390.png`，均在检查带接近共同位置、三个孔显出同步偏差但尚未整体对齐时采集并人工检查。
- **仍缺证据**：未知答案玩家能否把六孔理解成整体滤镜而不是六个独立按钮，以及两种纸波在小屏上是否能被自然看成不同周期而非装饰曲线。
- **停止结论**：工程硬门已通过；不增加节拍数字、音频输入、交点编号、六次点击彩蛋、逐孔拖动或数学最小公倍数说明，进入 100。

### 100 `silent-constellation` — 工程验收通过

- **设计事实**：左右各三颗纸星最初分散在暮色纸面两侧，单独点击不会给出答案；两组星群向内聚拢后，纸面负空间形成一个 V 形缺口。玩家必须从左上结构星连续经过底部交点再到右上结构星完成一笔轨迹，而不是描摹任意 V 字或点击三点。
- **实现结果**：以两组可真实拖动的三颗星、聚拢后的纸面负空间、三颗结构星和连续路径判定替换旧版“点两组图标 + 显式虚线 V”。点击星群、改变窗口尺寸和在任意位置画 V 均不推进；两组都进入宽松聚拢区后才开放轨迹层，轨迹必须依次命中三个结构区域且保有足够路径长度。键盘路线用左右方向键分别聚拢星群，依次选择三颗结构星，再持续按住 `V` 700ms；提前松开会取消。摄像头手势仅保留为可选增强，不是完成关卡的必要条件，页面内路线完整可用。
- **信息层级**：未破解前不显示关卡名、V 字答案、虚线路径、方向箭头、拖动步骤或摄像头要求；两组星群的对称关系、聚拢后的纸面切口和三颗结构星承担推理。桌面场景位于秒表右侧，手机位于主按钮下方，均未遮挡挑战文案、秒表或主按钮。
- **自动化证据**：
  - `pnpm lint`：通过。
  - `pnpm typecheck`：通过。
  - `pnpm vitest run tests/component/v2-puzzle-scene.test.tsx`：504 / 504 通过，其中 6 项覆盖专属结构、点击与缩放失败、单组聚拢、任意轨迹拒绝、正确连续轨迹和键盘 700ms 等价路线。
  - Playwright `desktop-1440` 与 `mobile-390` 专属验收：2 / 2 通过；验证真实点击无效、两组真实拖动聚拢、任意轨迹拒绝、连续结构轨迹完成、视口边界和核心区不遮挡。
  - 截图：`artifacts/screenshots/v2-sequential/100-desktop-1440.png`、`100-mobile-390.png`，均在两组星群已聚拢、负空间已经形成但尚未描完时采集并人工检查；桌面和手机层级清晰、场景完整可见。
- **仍缺证据**：未知答案玩家能否把两组星群自然理解为可聚拢对象，并从负空间而非星点本身推断连续 V 路线；物理手机上的单指轨迹手感；可选摄像头路线在本关场景中的真人体验未重新验证。
- **停止结论**：100 关逐关工程硬门全部通过；不增加显式 V 轮廓、轨迹虚线、三点编号、瞬时 `V` 键答案或强制摄像头权限，进入全量回归和发布前验证。

## 4. 100 关发布前回归（2026-08-11）

- `pnpm check:deploy`、`pnpm prisma:validate`、`pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过。
- `pnpm test`：20 文件、614 / 614 通过；其中本台账逐关组件合同 504 / 504。
- `pnpm test:integration:safe`：1 文件、3 / 3 通过，无数据库写入。
- 100 关桌面生产遍历：1 / 1 通过，确认 100 / 100 无 fallback、水平溢出、操作面越界或主按钮拦截；该审计发现并修复了 040 票据与 050 双语句带的透明命中框越界，并正确把 086 的真实菜单按钮识别为外部操作入口。
- 001–012 作者关键路径：1 / 1 聚合审计通过；旧第 006 关选择器已更新为专属擦雾画布。
- 代表关 001、003、012、040、069、100：桌面 1440 与手机 360 截图审计 2 / 2 通过。
- 完整页面、菜单、提示、计时结果隔离、收藏状态与 Axe：1440、360、390、768、reduced-motion、WebKit 共 6 / 6 通过，无 serious/critical 违规。
- 受本轮修复影响的 040、050、086、100：桌面 1440 与手机 390 专属真实交互复测 8 / 8 通过。
- 旧“22 类通用控制器快捷路线”已标记为由 100 个专属逐关合同取代，不再使用统一 `Interactive scene piece`、点击星群或瞬时 `V` 等与生产设计冲突的捷径。
- Playwright 服务由长时间运行不稳定的开发服务器改为生产构建服务；完整 `pnpm test:e2e` 一次性枚举 630 个跨项目项并取得 210 通过、420 条件跳过、0 失败。420 个跳过项是非适用项目条件和已淘汰的旧泛化审计，不计作通过。
- 完整回归额外发现并修复：008 透明中间片桌面越界、025 暗拍边界竞态、051 叶片旋转后不足 44px、053 重叠纸环的真实命中点不稳定、067 对 120ms 中间态的脆弱断言、073 长按释放竞态，以及 023/073 在 React 状态更新器内触发父回调的渲染期警告。
- 当前 `pnpm db:check` 因本机 5432 SSH 隧道未开启返回 `ECONNREFUSED`；本轮没有修改 `src/game` 关卡注册表、Prisma schema 或种子目录，因此未在未知数据库状态下执行同步写入。恢复隧道后仍需运行一次只读 `pnpm db:check`。
